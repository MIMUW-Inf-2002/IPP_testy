#ifdef NDEBUG
#undef NDEBUG
#endif

#include "phone_forward.h"

#include <assert.h>
#include <string.h>

#define MAX_LEN 30
int main() {
  char num1[MAX_LEN + 1], num2[MAX_LEN + 1];
  PhoneForward *pf;
  PhoneNumbers *pnum;
  pf = phfwdNew();
  pnum = phfwdGet(pf, "0");
  assert(strcmp(phnumGet(pnum, 0), "0") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "200");
  strcpy(num1, "1112");
  strcpy(num2, "02110");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "10020");
  assert(strcmp(phnumGet(pnum, 0), "10020") == 0);
  phnumDelete(pnum);
  strcpy(num1, "110");
  strcpy(num2, "0121");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "110");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "21112");
  assert(strcmp(phnumGet(pnum, 0), "21112") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "11");
  strcpy(num1, "1020");
  strcpy(num2, "20");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "12");
  pnum = phfwdGet(pf, "1020");
  phnumDelete(pnum);
  phfwdRemove(pf, "02");
  phfwdRemove(pf, "2101");
  pnum = phfwdGet(pf, "10");
  assert(strcmp(phnumGet(pnum, 0), "10") == 0);
  phnumDelete(pnum);
  strcpy(num1, "10");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2211");
  strcpy(num2, "2212");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0210");
  strcpy(num2, "10");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0");
  pnum = phfwdGet(pf, "2221");
  assert(strcmp(phnumGet(pnum, 0), "2221") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "1222");
  phfwdRemove(pf, "2212");
  pnum = phfwdGet(pf, "11201");
  assert(strcmp(phnumGet(pnum, 0), "11201") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1111");
  assert(strcmp(phnumGet(pnum, 0), "1111") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "20122");
  assert(strcmp(phnumGet(pnum, 0), "20122") == 0);
  phnumDelete(pnum);
  strcpy(num1, "22");
  strcpy(num2, "2110");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "10022");
  phfwdRemove(pf, "21011");
  phfwdRemove(pf, "1221");
  pnum = phfwdGet(pf, "102");
  assert(strcmp(phnumGet(pnum, 0), "12") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "022");
  assert(strcmp(phnumGet(pnum, 0), "022") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1012");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "1");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "210");
  assert(strcmp(phnumGet(pnum, 0), "210") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2220");
  assert(strcmp(phnumGet(pnum, 0), "211020") == 0);
  phnumDelete(pnum);
  strcpy(num1, "110");
  strcpy(num2, "01010");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "21");
  strcpy(num2, "012");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "00211");
  phfwdRemove(pf, "1022");
  phfwdRemove(pf, "00");
  pnum = phfwdGet(pf, "0002");
  assert(strcmp(phnumGet(pnum, 0), "0002") == 0);
  phnumDelete(pnum);
  strcpy(num1, "22");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2102");
  pnum = phfwdGet(pf, "1");
  phnumDelete(pnum);
  phfwdRemove(pf, "0");
  phfwdRemove(pf, "10222");
  strcpy(num1, "2221");
  strcpy(num2, "11210");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "20");
  phfwdRemove(pf, "20");
  phfwdRemove(pf, "0");
  strcpy(num1, "01120");
  strcpy(num2, "10202");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "10");
  phnumDelete(pnum);
  strcpy(num1, "0");
  strcpy(num2, "221");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "01");
  strcpy(num1, "010");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "01");
  strcpy(num2, "0222");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  strcpy(num1, "12");
  strcpy(num2, "01000");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "212");
  strcpy(num2, "20011");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "20212");
  assert(strcmp(phnumGet(pnum, 0), "20212") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "200");
  assert(strcmp(phnumGet(pnum, 0), "200") == 0);
  phnumDelete(pnum);
  strcpy(num1, "10");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "2202");
  assert(strcmp(phnumGet(pnum, 0), "102") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "22");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "02220");
  assert(strcmp(phnumGet(pnum, 0), "2212220") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2011");
  phfwdRemove(pf, "2");
  phfwdRemove(pf, "02201");
  phfwdRemove(pf, "2");
  phfwdRemove(pf, "2");
  pnum = phfwdGet(pf, "112");
  assert(strcmp(phnumGet(pnum, 0), "112") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "10");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1201");
  assert(strcmp(phnumGet(pnum, 0), "0100001") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "00");
  phfwdRemove(pf, "011");
  phfwdRemove(pf, "10");
  strcpy(num1, "1");
  strcpy(num2, "0111");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2");
  pnum = phfwdGet(pf, "10120");
  assert(strcmp(phnumGet(pnum, 0), "01110120") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "00012");
  assert(strcmp(phnumGet(pnum, 0), "2210012") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2");
  strcpy(num2, "1202");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "21211");
  assert(strcmp(phnumGet(pnum, 0), "12021211") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "1000");
  phfwdRemove(pf, "1");
  phfwdRemove(pf, "11");
  pnum = phfwdGet(pf, "120");
  assert(strcmp(phnumGet(pnum, 0), "120") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "00220");
  strcpy(num1, "100");
  strcpy(num2, "102");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "21022");
  strcpy(num1, "10");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2020");
  strcpy(num2, "22221");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "121");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "002");
  assert(strcmp(phnumGet(pnum, 0), "22102") == 0);
  phnumDelete(pnum);
  strcpy(num1, "0");
  strcpy(num2, "01");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "21022");
  strcpy(num2, "0112");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "11112");
  strcpy(num2, "002");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "20111");
  strcpy(num2, "11220");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "200");
  strcpy(num2, "01110");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2");
  strcpy(num1, "1222");
  strcpy(num2, "12101");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  strcpy(num1, "20");
  strcpy(num2, "020");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "11");
  strcpy(num1, "21121");
  strcpy(num2, "210");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0112");
  assert(strcmp(phnumGet(pnum, 0), "022212") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "0211");
  assert(strcmp(phnumGet(pnum, 0), "01211") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1110");
  assert(strcmp(phnumGet(pnum, 0), "1110") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "0101");
  assert(strcmp(phnumGet(pnum, 0), "11") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "0");
  phfwdRemove(pf, "210");
  strcpy(num1, "1");
  strcpy(num2, "000");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "110");
  assert(strcmp(phnumGet(pnum, 0), "00010") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2111");
  assert(strcmp(phnumGet(pnum, 0), "2111") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "1000");
  phfwdRemove(pf, "00001");
  phfwdRemove(pf, "100");
  strcpy(num1, "0");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "10");
  strcpy(num2, "202");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "12112");
  strcpy(num2, "212");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1120");
  pnum = phfwdGet(pf, "0201");
  assert(strcmp(phnumGet(pnum, 0), "1201") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "211");
  assert(strcmp(phnumGet(pnum, 0), "211") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "11122");
  assert(strcmp(phnumGet(pnum, 0), "0001122") == 0);
  phnumDelete(pnum);
  strcpy(num1, "01");
  strcpy(num2, "121");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "2");
  assert(strcmp(phnumGet(pnum, 0), "2") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2202");
  strcpy(num2, "0210");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "112");
  strcpy(num1, "2");
  strcpy(num2, "211");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "21");
  strcpy(num2, "20");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "22");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "2010");
  assert(strcmp(phnumGet(pnum, 0), "02010") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "1102");
  phfwdRemove(pf, "2211");
  strcpy(num1, "220");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "2112");
  assert(strcmp(phnumGet(pnum, 0), "2012") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "1100");
  strcpy(num1, "1222");
  strcpy(num2, "01102");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "12212");
  phfwdRemove(pf, "2");
  strcpy(num1, "0");
  strcpy(num2, "12212");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "22");
  strcpy(num2, "0002");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "21");
  assert(strcmp(phnumGet(pnum, 0), "21") == 0);
  phnumDelete(pnum);
  strcpy(num1, "102");
  strcpy(num2, "110");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "22210");
  assert(strcmp(phnumGet(pnum, 0), "0002210") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "00001");
  assert(strcmp(phnumGet(pnum, 0), "122120001") == 0);
  phnumDelete(pnum);
  strcpy(num1, "20012");
  strcpy(num2, "211");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0111");
  assert(strcmp(phnumGet(pnum, 0), "12111") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "1");
  strcpy(num1, "210");
  strcpy(num2, "112");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1111");
  assert(strcmp(phnumGet(pnum, 0), "1111") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2");
  strcpy(num2, "10");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "02");
  assert(strcmp(phnumGet(pnum, 0), "122122") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2021");
  pnum = phfwdGet(pf, "201");
  assert(strcmp(phnumGet(pnum, 0), "1001") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "22112");
  assert(strcmp(phnumGet(pnum, 0), "0002112") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2000");
  strcpy(num2, "100");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "1");
  strcpy(num2, "0000");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "010");
  strcpy(num2, "01110");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "02");
  assert(strcmp(phnumGet(pnum, 0), "122122") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "22100");
  assert(strcmp(phnumGet(pnum, 0), "0002100") == 0);
  phnumDelete(pnum);
  strcpy(num1, "20201");
  strcpy(num2, "21102");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "00");
  pnum = phfwdGet(pf, "020");
  assert(strcmp(phnumGet(pnum, 0), "1221220") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2122");
  assert(strcmp(phnumGet(pnum, 0), "10122") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2121");
  strcpy(num2, "1010");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "2");
  phnumDelete(pnum);
  phfwdRemove(pf, "02220");
  pnum = phfwdGet(pf, "1010");
  assert(strcmp(phnumGet(pnum, 0), "0000010") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "0");
  strcpy(num1, "02");
  strcpy(num2, "01");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "20001");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2100");
  strcpy(num2, "1222");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1011");
  assert(strcmp(phnumGet(pnum, 0), "0000011") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "120");
  assert(strcmp(phnumGet(pnum, 0), "000020") == 0);
  phnumDelete(pnum);
  strcpy(num1, "20");
  strcpy(num2, "222");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0");
  strcpy(num2, "a211");
  assert(phfwdAdd(pf, num1, num2) == false);
  pnum = phfwdGet(pf, "002");
  assert(strcmp(phnumGet(pnum, 0), "002") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1");
  phnumDelete(pnum);
  strcpy(num1, "1");
  strcpy(num2, "2202");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "110");
  assert(strcmp(phnumGet(pnum, 0), "220210") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "0202");
  assert(strcmp(phnumGet(pnum, 0), "0102") == 0);
  phnumDelete(pnum);
  strcpy(num1, "001");
  strcpy(num2, "12101");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2220");
  strcpy(num1, "01121");
  strcpy(num2, "102");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2");
  strcpy(num2, "210");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "22");
  pnum = phfwdGet(pf, "1001");
  assert(strcmp(phnumGet(pnum, 0), "2202001") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "001");
  phnumDelete(pnum);
  phfwdRemove(pf, "02211");
  pnum = phfwdGet(pf, "210");
  phnumDelete(pnum);
  strcpy(num1, "12001");
  strcpy(num2, "00001");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "00211");
  assert(strcmp(phnumGet(pnum, 0), "00211") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2222");
  strcpy(num1, "101");
  strcpy(num2, "11110");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "12200");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "2020");
  assert(strcmp(phnumGet(pnum, 0), "22220") == 0);
  phnumDelete(pnum);
  strcpy(num1, "20");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "10");
  strcpy(num1, "1");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "201");
  strcpy(num2, "21a");
  assert(phfwdAdd(pf, num1, num2) == false);
  strcpy(num1, "10");
  strcpy(num2, "12222");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "02222");
  phfwdRemove(pf, "22");
  pnum = phfwdGet(pf, "2002");
  assert(strcmp(phnumGet(pnum, 0), "002") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "02");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "10");
  phnumDelete(pnum);
  strcpy(num1, "2");
  strcpy(num2, "1102");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "220");
  assert(strcmp(phnumGet(pnum, 0), "110220") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "1");
  phfwdRemove(pf, "002");
  pnum = phfwdGet(pf, "0220");
  assert(strcmp(phnumGet(pnum, 0), "0120") == 0);
  phnumDelete(pnum);
  strcpy(num1, "10");
  strcpy(num2, "1100");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "200");
  strcpy(num2, "2002");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "12");
  phfwdRemove(pf, "2112");
  pnum = phfwdGet(pf, "1112");
  assert(strcmp(phnumGet(pnum, 0), "1112") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2");
  pnum = phfwdGet(pf, "000");
  assert(strcmp(phnumGet(pnum, 0), "000") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "02110");
  phfwdRemove(pf, "0");
  strcpy(num1, "0120");
  strcpy(num2, "012");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "01");
  strcpy(num2, "1212");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "000");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "211");
  assert(strcmp(phnumGet(pnum, 0), "211") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2211");
  strcpy(num2, "0010");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "21011");
  phfwdRemove(pf, "22202");
  strcpy(num1, "01212");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1000");
  assert(strcmp(phnumGet(pnum, 0), "110000") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2");
  strcpy(num2, "20110");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1011");
  pnum = phfwdGet(pf, "11100");
  assert(strcmp(phnumGet(pnum, 0), "11100") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "1");
  pnum = phfwdGet(pf, "22010");
  assert(strcmp(phnumGet(pnum, 0), "201102010") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "201");
  assert(strcmp(phnumGet(pnum, 0), "2011001") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "20");
  pnum = phfwdGet(pf, "210");
  assert(strcmp(phnumGet(pnum, 0), "2011010") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "10");
  assert(strcmp(phnumGet(pnum, 0), "10") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2");
  strcpy(num2, "12102");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2020");
  strcpy(num2, "220");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "220");
  phfwdRemove(pf, "00");
  strcpy(num1, "222");
  strcpy(num2, "22");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "20");
  assert(strcmp(phnumGet(pnum, 0), "121020") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "21101");
  assert(strcmp(phnumGet(pnum, 0), "121021101") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "1");
  phfwdRemove(pf, "0112");
  strcpy(num1, "12211");
  strcpy(num2, "121");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0110");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0");
  assert(strcmp(phnumGet(pnum, 0), "0") == 0);
  phnumDelete(pnum);
  strcpy(num1, "0101");
  strcpy(num2, "202");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "10200");
  assert(strcmp(phnumGet(pnum, 0), "10200") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "12");
  pnum = phfwdGet(pf, "21");
  assert(strcmp(phnumGet(pnum, 0), "121021") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "202");
  assert(strcmp(phnumGet(pnum, 0), "1210202") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "00");
  assert(strcmp(phnumGet(pnum, 0), "00") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2");
  strcpy(num2, "2101");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "01");
  phnumDelete(pnum);
  strcpy(num1, "000");
  strcpy(num2, "001");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0");
  strcpy(num1, "100");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1102");
  assert(strcmp(phnumGet(pnum, 0), "1102") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "120");
  phfwdRemove(pf, "2202");
  pnum = phfwdGet(pf, "00021");
  assert(strcmp(phnumGet(pnum, 0), "00021") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "012");
  assert(strcmp(phnumGet(pnum, 0), "012") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "02020");
  assert(strcmp(phnumGet(pnum, 0), "02020") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1210");
  assert(strcmp(phnumGet(pnum, 0), "1210") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "122");
  assert(strcmp(phnumGet(pnum, 0), "122") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "01020");
  pnum = phfwdGet(pf, "011");
  assert(strcmp(phnumGet(pnum, 0), "011") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "10");
  assert(strcmp(phnumGet(pnum, 0), "10") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "100");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "121");
  assert(strcmp(phnumGet(pnum, 0), "121") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2010");
  assert(strcmp(phnumGet(pnum, 0), "2101010") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "12212");
  assert(strcmp(phnumGet(pnum, 0), "12212") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  strcpy(num1, "11100");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0020");
  strcpy(num2, "000");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "022");
  pnum = phfwdGet(pf, "21001");
  assert(strcmp(phnumGet(pnum, 0), "01001") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2021");
  assert(strcmp(phnumGet(pnum, 0), "0021") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1122");
  assert(strcmp(phnumGet(pnum, 0), "1122") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1221");
  assert(strcmp(phnumGet(pnum, 0), "1221") == 0);
  phnumDelete(pnum);
  strcpy(num1, "212");
  strcpy(num2, "021");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "1222");
  strcpy(num2, "10");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "101");
  strcpy(num2, "20");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "010");
  assert(strcmp(phnumGet(pnum, 0), "210") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "01020");
  pnum = phfwdGet(pf, "002");
  assert(strcmp(phnumGet(pnum, 0), "202") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "201");
  assert(strcmp(phnumGet(pnum, 0), "001") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "0");
  pnum = phfwdGet(pf, "20");
  assert(strcmp(phnumGet(pnum, 0), "00") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "022");
  assert(strcmp(phnumGet(pnum, 0), "022") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2111");
  strcpy(num2, "21");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "1021");
  strcpy(num2, "10120");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "20");
  strcpy(num2, "220");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0");
  pnum = phfwdGet(pf, "101");
  phnumDelete(pnum);
  strcpy(num1, "01221");
  strcpy(num2, "2212");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "210");
  strcpy(num1, "10222");
  strcpy(num2, "2221");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "00");
  assert(strcmp(phnumGet(pnum, 0), "00") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "01");
  assert(strcmp(phnumGet(pnum, 0), "01") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1210");
  strcpy(num2, "00");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "21");
  assert(strcmp(phnumGet(pnum, 0), "01") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2");
  strcpy(num2, "11200");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2102");
  pnum = phfwdGet(pf, "12121");
  assert(strcmp(phnumGet(pnum, 0), "12121") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "22");
  strcpy(num1, "01012");
  strcpy(num2, "101");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0");
  assert(strcmp(phnumGet(pnum, 0), "0") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "00");
  pnum = phfwdGet(pf, "0121");
  assert(strcmp(phnumGet(pnum, 0), "0121") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1112");
  assert(strcmp(phnumGet(pnum, 0), "1112") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "122");
  pnum = phfwdGet(pf, "12");
  assert(strcmp(phnumGet(pnum, 0), "12") == 0);
  phnumDelete(pnum);
  strcpy(num1, "20");
  strcpy(num2, "12");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0120");
  strcpy(num1, "02210");
  strcpy(num2, "10");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "1120");
  strcpy(num2, "11");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "110");
  assert(strcmp(phnumGet(pnum, 0), "110") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "00022");
  strcpy(num1, "012");
  strcpy(num2, "12112");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0");
  strcpy(num2, "0000");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "01000");
  assert(strcmp(phnumGet(pnum, 0), "00001000") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  strcpy(num1, "11002");
  strcpy(num2, "22");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0212");
  strcpy(num2, "22");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "2");
  phnumDelete(pnum);
  strcpy(num1, "10010");
  strcpy(num2, "211");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "00020");
  pnum = phfwdGet(pf, "21");
  assert(strcmp(phnumGet(pnum, 0), "112001") == 0);
  phnumDelete(pnum);
  strcpy(num1, "21");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "20");
  phnumDelete(pnum);
  phfwdRemove(pf, "020");
  phfwdRemove(pf, "22220");
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  strcpy(num1, "0");
  strcpy(num2, "0111");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "100");
  phnumDelete(pnum);
  phfwdRemove(pf, "0101");
  phfwdRemove(pf, "10222");
  phfwdRemove(pf, "0");
  pnum = phfwdGet(pf, "2002");
  assert(strcmp(phnumGet(pnum, 0), "1202") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2202");
  strcpy(num1, "0");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "20012");
  strcpy(num1, "220");
  strcpy(num2, "10");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "2211");
  assert(strcmp(phnumGet(pnum, 0), "11200211") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  phfwdRemove(pf, "11");
  strcpy(num1, "011");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "02");
  assert(strcmp(phnumGet(pnum, 0), "02") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1212");
  assert(strcmp(phnumGet(pnum, 0), "1212") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "221");
  pnum = phfwdGet(pf, "02");
  assert(strcmp(phnumGet(pnum, 0), "02") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2");
  phfwdRemove(pf, "01");
  strcpy(num1, "1221");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "22010");
  pnum = phfwdGet(pf, "1122");
  assert(strcmp(phnumGet(pnum, 0), "1122") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "11212");
  assert(strcmp(phnumGet(pnum, 0), "11212") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "02210");
  strcpy(num1, "0");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "22");
  assert(strcmp(phnumGet(pnum, 0), "22") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "011");
  assert(strcmp(phnumGet(pnum, 0), "211") == 0);
  phnumDelete(pnum);
  strcpy(num1, "22");
  strcpy(num2, "00002");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "12222");
  phfwdRemove(pf, "11");
  phfwdRemove(pf, "00");
  pnum = phfwdGet(pf, "2211");
  assert(strcmp(phnumGet(pnum, 0), "0000211") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "1202");
  strcpy(num1, "10200");
  strcpy(num2, "01");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "11012");
  assert(strcmp(phnumGet(pnum, 0), "11012") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "02221");
  strcpy(num1, "2102");
  strcpy(num2, "12");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "22222");
  assert(strcmp(phnumGet(pnum, 0), "00002222") == 0);
  phnumDelete(pnum);
  strcpy(num1, "21020");
  strcpy(num2, "00010");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "12122");
  strcpy(num2, "11");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "00202");
  strcpy(num1, "21");
  strcpy(num2, "01");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "12211");
  strcpy(num2, "111");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "00012");
  pnum = phfwdGet(pf, "20011");
  assert(strcmp(phnumGet(pnum, 0), "00011") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2022");
  assert(strcmp(phnumGet(pnum, 0), "0022") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1120");
  assert(strcmp(phnumGet(pnum, 0), "1120") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "02");
  phfwdRemove(pf, "0");
  strcpy(num1, "02");
  strcpy(num2, "221");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "20121");
  pnum = phfwdGet(pf, "01011");
  assert(strcmp(phnumGet(pnum, 0), "01011") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2120");
  pnum = phfwdGet(pf, "10011");
  assert(strcmp(phnumGet(pnum, 0), "111") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2221");
  assert(strcmp(phnumGet(pnum, 0), "0000221") == 0);
  phnumDelete(pnum);
  strcpy(num1, "0201");
  strcpy(num2, "2022");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "11");
  phfwdRemove(pf, "2");
  phfwdRemove(pf, "120");
  strcpy(num1, "0111");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "02102");
  assert(strcmp(phnumGet(pnum, 0), "221102") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "21021");
  assert(strcmp(phnumGet(pnum, 0), "21021") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  strcpy(num1, "10");
  strcpy(num2, "220");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "122");
  strcpy(num1, "01");
  strcpy(num2, "22100");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1022");
  assert(strcmp(phnumGet(pnum, 0), "22022") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "11110");
  pnum = phfwdGet(pf, "21");
  assert(strcmp(phnumGet(pnum, 0), "21") == 0);
  phnumDelete(pnum);
  strcpy(num1, "122");
  strcpy(num2, "2122");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "11001");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0");
  assert(strcmp(phnumGet(pnum, 0), "0") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "00010");
  pnum = phfwdGet(pf, "0");
  assert(strcmp(phnumGet(pnum, 0), "0") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2012");
  strcpy(num2, "110");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "00011");
  phfwdRemove(pf, "11");
  phfwdRemove(pf, "01");
  phfwdRemove(pf, "00");
  phfwdRemove(pf, "12022");
  pnum = phfwdGet(pf, "0");
  assert(strcmp(phnumGet(pnum, 0), "0") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1020");
  strcpy(num2, "220");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2");
  pnum = phfwdGet(pf, "22110");
  assert(strcmp(phnumGet(pnum, 0), "22110") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "00111");
  assert(strcmp(phnumGet(pnum, 0), "00111") == 0);
  phnumDelete(pnum);
  strcpy(num1, "01");
  strcpy(num2, "000");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "222");
  assert(strcmp(phnumGet(pnum, 0), "222") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "11");
  strcpy(num1, "2");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  strcpy(num1, "02011");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "2101");
  assert(strcmp(phnumGet(pnum, 0), "2101") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1");
  strcpy(num2, "011");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "01");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "121");
  assert(strcmp(phnumGet(pnum, 0), "01121") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "111");
  assert(strcmp(phnumGet(pnum, 0), "01111") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "10112");
  assert(strcmp(phnumGet(pnum, 0), "2012") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1222");
  assert(strcmp(phnumGet(pnum, 0), "21222") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "01220");
  strcpy(num1, "2110");
  strcpy(num2, "200");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0");
  strcpy(num2, "00");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "110");
  assert(strcmp(phnumGet(pnum, 0), "01110") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "11");
  phfwdRemove(pf, "1");
  strcpy(num1, "11a0");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == false);
  pnum = phfwdGet(pf, "00");
  assert(strcmp(phnumGet(pnum, 0), "000") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "02");
  strcpy(num1, "2");
  strcpy(num2, "01");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1010");
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2");
  phfwdRemove(pf, "222");
  strcpy(num1, "2");
  strcpy(num2, "01020");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2");
  strcpy(num2, "121");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "122");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "12011");
  assert(strcmp(phnumGet(pnum, 0), "12011") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "11");
  phfwdRemove(pf, "20102");
  strcpy(num1, "10002");
  strcpy(num2, "110");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "12");
  assert(strcmp(phnumGet(pnum, 0), "12") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "22");
  assert(strcmp(phnumGet(pnum, 0), "1212") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "002");
  assert(strcmp(phnumGet(pnum, 0), "0002") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1120");
  strcpy(num2, "22");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "002");
  strcpy(num2, "01102");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "202");
  phfwdRemove(pf, "112");
  pnum = phfwdGet(pf, "21110");
  assert(strcmp(phnumGet(pnum, 0), "1211110") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2");
  phfwdRemove(pf, "002");
  strcpy(num1, "2020");
  strcpy(num2, "200");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "020");
  strcpy(num2, "010");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "20111");
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  strcpy(num1, "211");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "201");
  pnum = phfwdGet(pf, "21210");
  assert(strcmp(phnumGet(pnum, 0), "21210") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "122");
  phnumDelete(pnum);
  phfwdRemove(pf, "100");
  strcpy(num1, "10122");
  strcpy(num2, "20011");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "1");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "20");
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  strcpy(num1, "0001");
  strcpy(num2, "00");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0");
  strcpy(num2, "11112");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1");
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  strcpy(num1, "01220");
  strcpy(num2, "01002");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "121");
  assert(strcmp(phnumGet(pnum, 0), "121") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  phfwdRemove(pf, "00122");
  strcpy(num1, "220");
  strcpy(num2, "2021");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "10");
  strcpy(num1, "02");
  strcpy(num2, "20011");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "11122");
  phfwdRemove(pf, "02121");
  strcpy(num1, "1212");
  strcpy(num2, "1021");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "22");
  strcpy(num2, "02001");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "22100");
  strcpy(num2, "2200");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0012");
  pnum = phfwdGet(pf, "21");
  assert(strcmp(phnumGet(pnum, 0), "21") == 0);
  phnumDelete(pnum);
  strcpy(num1, "10");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "022");
  strcpy(num1, "1210");
  strcpy(num2, "0112");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "00");
  assert(strcmp(phnumGet(pnum, 0), "111120") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "1211");
  strcpy(num1, "1");
  strcpy(num2, "22");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "11");
  strcpy(num2, "01");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "110");
  assert(strcmp(phnumGet(pnum, 0), "010") == 0);
  phnumDelete(pnum);
  strcpy(num1, "0");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2111");
  pnum = phfwdGet(pf, "02");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "21222");
  assert(strcmp(phnumGet(pnum, 0), "21222") == 0);
  phnumDelete(pnum);
  strcpy(num1, "21002");
  strcpy(num2, "0000");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "012");
  strcpy(num2, "011");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "21101");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "21");
  strcpy(num2, "12");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "00");
  strcpy(num1, "21");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "20000");
  pnum = phfwdGet(pf, "021");
  assert(strcmp(phnumGet(pnum, 0), "200111") == 0);
  phnumDelete(pnum);
  strcpy(num1, "22112");
  strcpy(num2, "10");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "11");
  pnum = phfwdGet(pf, "01");
  phnumDelete(pnum);
  strcpy(num1, "2");
  strcpy(num2, "00");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0");
  strcpy(num2, "22022");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "02010");
  assert(strcmp(phnumGet(pnum, 0), "01010") == 0);
  phnumDelete(pnum);
  strcpy(num1, "01001");
  strcpy(num2, "11020");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0021");
  strcpy(num2, "11110");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "22");
  pnum = phfwdGet(pf, "0120");
  assert(strcmp(phnumGet(pnum, 0), "0110") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1");
  strcpy(num2, "11");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "10");
  strcpy(num2, "11021");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "2");
  phnumDelete(pnum);
  phfwdRemove(pf, "220");
  phfwdRemove(pf, "2");
  strcpy(num1, "120");
  strcpy(num2, "10");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "2211");
  assert(strcmp(phnumGet(pnum, 0), "2211") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1212");
  strcpy(num2, "112");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "110");
  pnum = phfwdGet(pf, "122");
  assert(strcmp(phnumGet(pnum, 0), "1122") == 0);
  phnumDelete(pnum);
  strcpy(num1, "10210");
  strcpy(num2, "2121");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2210");
  pnum = phfwdGet(pf, "12002");
  assert(strcmp(phnumGet(pnum, 0), "1002") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1212");
  strcpy(num2, "11102");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "1110");
  strcpy(num2, "02201");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "11101");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "11212");
  strcpy(num2, "12");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2");
  pnum = phfwdGet(pf, "02");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1200");
  assert(strcmp(phnumGet(pnum, 0), "100") == 0);
  phnumDelete(pnum);
  strcpy(num1, "202");
  strcpy(num2, "2110");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "010");
  strcpy(num1, "1");
  strcpy(num2, "10");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0");
  strcpy(num1, "10012");
  strcpy(num2, "0002");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "1");
  strcpy(num2, "20");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "000");
  strcpy(num2, "20");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "220");
  strcpy(num2, "0222");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "220");
  strcpy(num2, "221");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "21");
  strcpy(num2, "21020");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "22122");
  assert(strcmp(phnumGet(pnum, 0), "22122") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1");
  strcpy(num2, "121");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0");
  phfwdRemove(pf, "1");
  pnum = phfwdGet(pf, "0");
  assert(strcmp(phnumGet(pnum, 0), "0") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "01");
  assert(strcmp(phnumGet(pnum, 0), "01") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "22222");
  assert(strcmp(phnumGet(pnum, 0), "22222") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "00");
  assert(strcmp(phnumGet(pnum, 0), "00") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "211");
  pnum = phfwdGet(pf, "2");
  assert(strcmp(phnumGet(pnum, 0), "2") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "0");
  phfwdRemove(pf, "2");
  phfwdRemove(pf, "1");
  phfwdRemove(pf, "22012");
  phfwdRemove(pf, "00");
  pnum = phfwdGet(pf, "202");
  assert(strcmp(phnumGet(pnum, 0), "202") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2");
  assert(strcmp(phnumGet(pnum, 0), "2") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "1110");
  phfwdRemove(pf, "01");
  phfwdRemove(pf, "0");
  phfwdRemove(pf, "22212");
  pnum = phfwdGet(pf, "211");
  assert(strcmp(phnumGet(pnum, 0), "211") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "021");
  assert(strcmp(phnumGet(pnum, 0), "021") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "0");
  assert(strcmp(phnumGet(pnum, 0), "0") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "222");
  pnum = phfwdGet(pf, "2");
  assert(strcmp(phnumGet(pnum, 0), "2") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1");
  strcpy(num2, "202");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "020");
  assert(strcmp(phnumGet(pnum, 0), "020") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "100");
  assert(strcmp(phnumGet(pnum, 0), "20200") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "112");
  pnum = phfwdGet(pf, "2101");
  assert(strcmp(phnumGet(pnum, 0), "2101") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2");
  strcpy(num2, "11002");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "022");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0212");
  strcpy(num2, "22122");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "2210");
  assert(strcmp(phnumGet(pnum, 0), "11002210") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "122");
  assert(strcmp(phnumGet(pnum, 0), "20222") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "021");
  assert(strcmp(phnumGet(pnum, 0), "021") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2111");
  assert(strcmp(phnumGet(pnum, 0), "11002111") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "101");
  pnum = phfwdGet(pf, "1");
  phnumDelete(pnum);
  strcpy(num1, "11");
  strcpy(num2, "12110");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2");
  strcpy(num2, "211");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0");
  assert(strcmp(phnumGet(pnum, 0), "0") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "00120");
  assert(strcmp(phnumGet(pnum, 0), "00120") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "0210");
  pnum = phfwdGet(pf, "02120");
  assert(strcmp(phnumGet(pnum, 0), "221220") == 0);
  phnumDelete(pnum);
  strcpy(num1, "20");
  strcpy(num2, "00");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "12");
  phfwdRemove(pf, "010");
  strcpy(num1, "0");
  strcpy(num2, "000");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "02221");
  phfwdRemove(pf, "01220");
  pnum = phfwdGet(pf, "020");
  assert(strcmp(phnumGet(pnum, 0), "00020") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2");
  strcpy(num2, "2200");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "21");
  assert(strcmp(phnumGet(pnum, 0), "22001") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  strcpy(num1, "2101");
  strcpy(num2, "2011");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "21120");
  strcpy(num2, "00");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "020");
  assert(strcmp(phnumGet(pnum, 0), "00020") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1220");
  assert(strcmp(phnumGet(pnum, 0), "202220") == 0);
  phnumDelete(pnum);
  strcpy(num1, "110");
  strcpy(num2, "1221");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "21");
  phfwdRemove(pf, "1");
  strcpy(num1, "1121");
  strcpy(num2, "11111");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "000");
  strcpy(num2, "122");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "20");
  strcpy(num2, "221");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0100");
  assert(strcmp(phnumGet(pnum, 0), "000100") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2");
  strcpy(num2, "2021");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "22");
  assert(strcmp(phnumGet(pnum, 0), "20212") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "22");
  assert(strcmp(phnumGet(pnum, 0), "20212") == 0);
  phnumDelete(pnum);
  strcpy(num1, "22");
  strcpy(num2, "11200");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "21112");
  strcpy(num1, "1");
  strcpy(num2, "20011");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2");
  strcpy(num2, "21110");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "212");
  assert(strcmp(phnumGet(pnum, 0), "2111012") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2100");
  assert(strcmp(phnumGet(pnum, 0), "21110100") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2");
  strcpy(num2, "12002");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "120");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "1");
  strcpy(num2, "12");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1002");
  assert(strcmp(phnumGet(pnum, 0), "12002") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "00");
  assert(strcmp(phnumGet(pnum, 0), "0000") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "1120");
  phfwdRemove(pf, "210");
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "110");
  assert(strcmp(phnumGet(pnum, 0), "1210") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "202");
  assert(strcmp(phnumGet(pnum, 0), "2212") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2");
  phnumDelete(pnum);
  phfwdRemove(pf, "012");
  pnum = phfwdGet(pf, "10");
  assert(strcmp(phnumGet(pnum, 0), "120") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2a");
  phfwdRemove(pf, "202");
  phfwdRemove(pf, "1");
  phfwdRemove(pf, "1012");
  pnum = phfwdGet(pf, "020");
  assert(strcmp(phnumGet(pnum, 0), "00020") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "02001");
  assert(strcmp(phnumGet(pnum, 0), "0002001") == 0);
  phnumDelete(pnum);
  strcpy(num1, "21");
  strcpy(num2, "202");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "11100");
  phfwdRemove(pf, "1121");
  phfwdRemove(pf, "20211");
  strcpy(num1, "2021");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0112");
  assert(strcmp(phnumGet(pnum, 0), "000112") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1120");
  strcpy(num2, "001");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0021");
  strcpy(num1, "101");
  strcpy(num2, "210");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1111");
  phfwdRemove(pf, "02000");
  strcpy(num1, "21");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1102");
  assert(strcmp(phnumGet(pnum, 0), "1102") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "22120");
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  strcpy(num1, "202");
  strcpy(num2, "0110");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0111");
  assert(strcmp(phnumGet(pnum, 0), "000111") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "20101");
  strcpy(num1, "2");
  strcpy(num2, "11202");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "02110");
  strcpy(num1, "02");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "11");
  phfwdRemove(pf, "2221");
  pnum = phfwdGet(pf, "2");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "01222");
  assert(strcmp(phnumGet(pnum, 0), "0001222") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "0");
  phfwdRemove(pf, "01");
  phfwdRemove(pf, "2");
  phfwdRemove(pf, "2");
  phfwdRemove(pf, "211");
  pnum = phfwdGet(pf, "0112");
  assert(strcmp(phnumGet(pnum, 0), "0112") == 0);
  phnumDelete(pnum);
  strcpy(num1, "210");
  strcpy(num2, "00");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "22");
  pnum = phfwdGet(pf, "10");
  assert(strcmp(phnumGet(pnum, 0), "10") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "11121");
  assert(strcmp(phnumGet(pnum, 0), "11121") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1020");
  assert(strcmp(phnumGet(pnum, 0), "1020") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "01220");
  assert(strcmp(phnumGet(pnum, 0), "01220") == 0);
  phnumDelete(pnum);
  strcpy(num1, "00010");
  strcpy(num2, "22");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "22");
  pnum = phfwdGet(pf, "02");
  assert(strcmp(phnumGet(pnum, 0), "02") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "021");
  phfwdRemove(pf, "22122");
  phfwdRemove(pf, "2221");
  strcpy(num1, "11110");
  strcpy(num2, "220");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "01");
  assert(strcmp(phnumGet(pnum, 0), "01") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "21");
  assert(strcmp(phnumGet(pnum, 0), "21") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "02");
  assert(strcmp(phnumGet(pnum, 0), "02") == 0);
  phnumDelete(pnum);
  strcpy(num1, "01021");
  strcpy(num2, "00");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "000");
  assert(strcmp(phnumGet(pnum, 0), "000") == 0);
  phnumDelete(pnum);
  strcpy(num1, "01021");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "101");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "1");
  strcpy(num2, "12102");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2220");
  pnum = phfwdGet(pf, "10010");
  assert(strcmp(phnumGet(pnum, 0), "121020010") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "01");
  pnum = phfwdGet(pf, "12022");
  assert(strcmp(phnumGet(pnum, 0), "121022022") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2020");
  strcpy(num2, "010");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "11");
  assert(strcmp(phnumGet(pnum, 0), "121021") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "001");
  strcpy(num1, "00011");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "221");
  strcpy(num1, "20");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "02");
  strcpy(num1, "1210");
  strcpy(num2, "12020");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "21");
  strcpy(num2, "2210");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2000");
  phfwdRemove(pf, "2111");
  pnum = phfwdGet(pf, "0122");
  assert(strcmp(phnumGet(pnum, 0), "0122") == 0);
  phnumDelete(pnum);
  strcpy(num1, "111");
  strcpy(num2, "1121");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "220");
  assert(strcmp(phnumGet(pnum, 0), "220") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2011");
  strcpy(num2, "1101");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "210");
  strcpy(num2, "201");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "12110");
  phfwdRemove(pf, "2001");
  pnum = phfwdGet(pf, "020");
  assert(strcmp(phnumGet(pnum, 0), "020") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "0212");
  strcpy(num1, "1002");
  strcpy(num2, "122");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "11");
  assert(strcmp(phnumGet(pnum, 0), "121021") == 0);
  phnumDelete(pnum);
  strcpy(num1, "01210");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "00020");
  strcpy(num1, "12000");
  strcpy(num2, "10102");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1121");
  strcpy(num1, "001");
  strcpy(num2, "1202");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "02");
  strcpy(num2, "11");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "01120");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0");
  strcpy(num2, "12");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "11");
  assert(strcmp(phnumGet(pnum, 0), "121021") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "22");
  phfwdRemove(pf, "2110");
  pnum = phfwdGet(pf, "00");
  assert(strcmp(phnumGet(pnum, 0), "120") == 0);
  phnumDelete(pnum);
  strcpy(num1, "00");
  strcpy(num2, "222");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "221");
  phfwdRemove(pf, "02020");
  phfwdRemove(pf, "0");
  pnum = phfwdGet(pf, "202");
  assert(strcmp(phnumGet(pnum, 0), "22") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "22112");
  assert(strcmp(phnumGet(pnum, 0), "22112") == 0);
  phnumDelete(pnum);
  strcpy(num1, "0");
  strcpy(num2, "20011");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "20020");
  assert(strcmp(phnumGet(pnum, 0), "2020") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2");
  assert(strcmp(phnumGet(pnum, 0), "2") == 0);
  phnumDelete(pnum);
  strcpy(num1, "0100");
  strcpy(num2, "211");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "21");
  phnumDelete(pnum);
  phfwdRemove(pf, "0220");
  strcpy(num1, "0011");
  strcpy(num2, "00");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "021");
  strcpy(num2, "1222");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "1");
  strcpy(num2, "00222");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "00212");
  assert(strcmp(phnumGet(pnum, 0), "200110212") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "02");
  pnum = phfwdGet(pf, "1");
  phnumDelete(pnum);
  strcpy(num1, "00");
  strcpy(num2, "22");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0");
  strcpy(num2, "0221");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "20");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "22");
  assert(strcmp(phnumGet(pnum, 0), "22") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "020");
  pnum = phfwdGet(pf, "020");
  assert(strcmp(phnumGet(pnum, 0), "022120") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "22");
  phfwdRemove(pf, "201");
  strcpy(num1, "11022");
  strcpy(num2, "22");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1100");
  assert(strcmp(phnumGet(pnum, 0), "00222100") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "01121");
  assert(strcmp(phnumGet(pnum, 0), "02211121") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2210");
  phfwdRemove(pf, "10011");
  phfwdRemove(pf, "0");
  strcpy(num1, "1012");
  strcpy(num2, "0220");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "12102");
  assert(strcmp(phnumGet(pnum, 0), "120202") == 0);
  phnumDelete(pnum);
  strcpy(num1, "222");
  strcpy(num2, "11202");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "01");
  assert(strcmp(phnumGet(pnum, 0), "01") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "0");
  assert(strcmp(phnumGet(pnum, 0), "0") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "00");
  assert(strcmp(phnumGet(pnum, 0), "00") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "112");
  strcpy(num1, "111");
  strcpy(num2, "22102");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0221");
  strcpy(num1, "021");
  strcpy(num2, "02110");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "21102");
  assert(strcmp(phnumGet(pnum, 0), "2210102") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2");
  strcpy(num1, "1");
  strcpy(num2, "100");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "22022");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "012");
  assert(strcmp(phnumGet(pnum, 0), "012") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "010");
  assert(strcmp(phnumGet(pnum, 0), "010") == 0);
  phnumDelete(pnum);
  strcpy(num1, "011");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1021");
  phfwdRemove(pf, "0020");
  strcpy(num1, "22110");
  strcpy(num2, "2201");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "21");
  strcpy(num2, "20220");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0212");
  assert(strcmp(phnumGet(pnum, 0), "021102") == 0);
  phnumDelete(pnum);
  strcpy(num1, "0");
  strcpy(num2, "10121");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "00");
  strcpy(num2, "1011");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "22201");
  strcpy(num2, "200");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "102");
  strcpy(num2, "21");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "211");
  assert(strcmp(phnumGet(pnum, 0), "202201") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "200");
  strcpy(num1, "11200");
  strcpy(num2, "20");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "2");
  assert(strcmp(phnumGet(pnum, 0), "2") == 0);
  phnumDelete(pnum);
  strcpy(num1, "0");
  strcpy(num2, "00");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "00");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "212");
  assert(strcmp(phnumGet(pnum, 0), "202202") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "0");
  strcpy(num1, "12022");
  strcpy(num2, "1221");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "20");
  strcpy(num2, "0112");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2");
  strcpy(num1, "01100");
  strcpy(num2, "22");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0");
  strcpy(num1, "01");
  strcpy(num2, "2002");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1");
  phfwdRemove(pf, "21012");
  strcpy(num1, "22001");
  strcpy(num2, "12");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0020");
  phfwdRemove(pf, "0");
  pnum = phfwdGet(pf, "0012");
  assert(strcmp(phnumGet(pnum, 0), "0012") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "222");
  strcpy(num1, "01222");
  strcpy(num2, "11");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "020");
  strcpy(num1, "010");
  strcpy(num2, "1100");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "10210");
  phfwdRemove(pf, "0");
  pnum = phfwdGet(pf, "2001");
  assert(strcmp(phnumGet(pnum, 0), "2001") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2111");
  strcpy(num1, "10112");
  strcpy(num2, "12");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "11");
  strcpy(num2, "2210");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "110");
  pnum = phfwdGet(pf, "12");
  assert(strcmp(phnumGet(pnum, 0), "12") == 0);
  phnumDelete(pnum);
  strcpy(num1, "21");
  strcpy(num2, "12210");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "21");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1211");
  strcpy(num2, "022");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1");
  phfwdRemove(pf, "1212");
  strcpy(num1, "1211");
  strcpy(num2, "20");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "02201");
  strcpy(num2, "1111");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1");
  phfwdRemove(pf, "1");
  strcpy(num1, "22110");
  strcpy(num2, "221");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0222");
  assert(strcmp(phnumGet(pnum, 0), "0222") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "10");
  assert(strcmp(phnumGet(pnum, 0), "10") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2");
  assert(strcmp(phnumGet(pnum, 0), "2") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1");
  strcpy(num2, "120");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0");
  assert(strcmp(phnumGet(pnum, 0), "0") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "00221");
  assert(strcmp(phnumGet(pnum, 0), "00221") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2012");
  assert(strcmp(phnumGet(pnum, 0), "2012") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "02220");
  assert(strcmp(phnumGet(pnum, 0), "02220") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "00111");
  pnum = phfwdGet(pf, "012");
  assert(strcmp(phnumGet(pnum, 0), "012") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "022");
  assert(strcmp(phnumGet(pnum, 0), "022") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2");
  assert(strcmp(phnumGet(pnum, 0), "2") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "20");
  assert(strcmp(phnumGet(pnum, 0), "20") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2200");
  assert(strcmp(phnumGet(pnum, 0), "2200") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "0");
  strcpy(num1, "20");
  strcpy(num2, "1211");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "100");
  strcpy(num2, "00111");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "112");
  strcpy(num1, "010");
  strcpy(num2, "20100");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "02");
  assert(strcmp(phnumGet(pnum, 0), "02") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "0110");
  assert(strcmp(phnumGet(pnum, 0), "0110") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "0010");
  assert(strcmp(phnumGet(pnum, 0), "0010") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "0011");
  assert(strcmp(phnumGet(pnum, 0), "0011") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2");
  strcpy(num1, "2122");
  strcpy(num2, "2122");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "110");
  phfwdRemove(pf, "21");
  phfwdRemove(pf, "01200");
  strcpy(num1, "11");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0");
  assert(strcmp(phnumGet(pnum, 0), "0") == 0);
  phnumDelete(pnum);
  strcpy(num1, "222");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2111");
  phfwdRemove(pf, "1");
  pnum = phfwdGet(pf, "0");
  assert(strcmp(phnumGet(pnum, 0), "0") == 0);
  phnumDelete(pnum);
  strcpy(num1, "12");
  strcpy(num2, "01");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "001");
  strcpy(num2, "20");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "01022");
  strcpy(num2, "2101");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "101a0");
  phfwdRemove(pf, "10011");
  phfwdRemove(pf, "1110");
  strcpy(num1, "0012");
  strcpy(num2, "022");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2022");
  strcpy(num1, "021");
  strcpy(num2, "100");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "2120");
  assert(strcmp(phnumGet(pnum, 0), "2120") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2101");
  assert(strcmp(phnumGet(pnum, 0), "2101") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "00");
  assert(strcmp(phnumGet(pnum, 0), "00") == 0);
  phnumDelete(pnum);
  strcpy(num1, "0100");
  strcpy(num2, "20121");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "12212");
  phfwdRemove(pf, "21212");
  phfwdRemove(pf, "221");
  phfwdRemove(pf, "12");
  strcpy(num1, "02");
  strcpy(num2, "10");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0");
  pnum = phfwdGet(pf, "211");
  assert(strcmp(phnumGet(pnum, 0), "211") == 0);
  phnumDelete(pnum);
  strcpy(num1, "211");
  strcpy(num2, "21");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "111");
  assert(strcmp(phnumGet(pnum, 0), "111") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "222");
  pnum = phfwdGet(pf, "20");
  assert(strcmp(phnumGet(pnum, 0), "20") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1120");
  strcpy(num2, "00");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1000");
  phfwdRemove(pf, "1100");
  strcpy(num1, "00");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "10");
  assert(strcmp(phnumGet(pnum, 0), "10") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "220");
  assert(strcmp(phnumGet(pnum, 0), "220") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "22221");
  assert(strcmp(phnumGet(pnum, 0), "22221") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2211");
  strcpy(num1, "12202");
  strcpy(num2, "11");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "10100");
  phfwdRemove(pf, "012");
  strcpy(num1, "1022");
  strcpy(num2, "2120");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "11012");
  assert(strcmp(phnumGet(pnum, 0), "11012") == 0);
  phnumDelete(pnum);
  strcpy(num1, "0022");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "00");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2");
  assert(strcmp(phnumGet(pnum, 0), "2") == 0);
  phnumDelete(pnum);
  strcpy(num1, "021");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "000");
  strcpy(num1, "01");
  strcpy(num2, "101");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "1112");
  strcpy(num2, "20");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2121");
  phfwdRemove(pf, "21110");
  strcpy(num1, "21");
  strcpy(num2, "2002");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1121");
  assert(strcmp(phnumGet(pnum, 0), "1121") == 0);
  phnumDelete(pnum);
  strcpy(num1, "221");
  strcpy(num2, "222");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "12");
  assert(strcmp(phnumGet(pnum, 0), "12") == 0);
  phnumDelete(pnum);
  strcpy(num1, "11222");
  strcpy(num2, "0102");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "102");
  assert(strcmp(phnumGet(pnum, 0), "102") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "10200");
  assert(strcmp(phnumGet(pnum, 0), "10200") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2");
  phfwdRemove(pf, "2011");
  strcpy(num1, "11221");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0");
  strcpy(num2, "0010");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "20");
  strcpy(num1, "22");
  strcpy(num2, "021");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "12212");
  assert(strcmp(phnumGet(pnum, 0), "12212") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2");
  strcpy(num1, "2");
  strcpy(num2, "2010");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "22210");
  strcpy(num2, "20001");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0012");
  phfwdRemove(pf, "1022");
  strcpy(num1, "0022");
  strcpy(num2, "22");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0220");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "10");
  assert(strcmp(phnumGet(pnum, 0), "10") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "10");
  phfwdRemove(pf, "2");
  pnum = phfwdGet(pf, "2221");
  assert(strcmp(phnumGet(pnum, 0), "2221") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2211");
  assert(strcmp(phnumGet(pnum, 0), "2211") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "0101");
  assert(strcmp(phnumGet(pnum, 0), "10101") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2");
  assert(strcmp(phnumGet(pnum, 0), "2") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "0");
  phfwdRemove(pf, "01202");
  strcpy(num1, "20201");
  strcpy(num2, "01112");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0");
  phfwdRemove(pf, "02220");
  phfwdRemove(pf, "110");
  pnum = phfwdGet(pf, "2212");
  assert(strcmp(phnumGet(pnum, 0), "2212") == 0);
  phnumDelete(pnum);
  strcpy(num1, "20");
  strcpy(num2, "011");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "11");
  phfwdRemove(pf, "0");
  phfwdRemove(pf, "221");
  strcpy(num1, "12");
  strcpy(num2, "00100");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1211");
  assert(strcmp(phnumGet(pnum, 0), "0010011") == 0);
  phnumDelete(pnum);
  strcpy(num1, "21");
  strcpy(num2, "1122");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0220");
  assert(strcmp(phnumGet(pnum, 0), "0220") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2002");
  strcpy(num2, "11100");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "1");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "21");
  phnumDelete(pnum);
  phfwdRemove(pf, "01212");
  strcpy(num1, "02");
  strcpy(num2, "210");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2022");
  strcpy(num2, "22000");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "102");
  strcpy(num2, "2011");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "2002");
  phnumDelete(pnum);
  strcpy(num1, "01");
  strcpy(num2, "2222");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "10");
  strcpy(num2, "20");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "1");
  strcpy(num2, "120");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "22011");
  strcpy(num2, "21010");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "202");
  strcpy(num1, "2212");
  strcpy(num2, "2212");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0");
  strcpy(num1, "102");
  strcpy(num2, "101");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdDelete(pf);
}
