#ifdef NDEBUG
#undef NDEBUG
#endif

#include "../../phone_forward.h"

#include <assert.h>
#include <string.h>

#define MAX_LEN 30
int main() {
  char num1[MAX_LEN + 1], num2[MAX_LEN + 1];
  PhoneForward *pf;
  PhoneNumbers *pnum;
  pf = phfwdNew();
  strcpy(num1, "20011");
  strcpy(num2, "20022");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "120");
  strcpy(num1, "000");
  strcpy(num2, "201");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "02102");
  strcpy(num2, "2010");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "11");
  phfwdRemove(pf, "01022");
  strcpy(num1, "2202");
  strcpy(num2, "11");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1002");
  assert(strcmp(phnumGet(pnum, 0), "1002") == 0);
  phnumDelete(pnum);
  strcpy(num1, "01");
  strcpy(num2, "120");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0");
  phfwdRemove(pf, "112");
  phfwdRemove(pf, "2");
  pnum = phfwdGet(pf, "0021");
  assert(strcmp(phnumGet(pnum, 0), "0021") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "02");
  assert(strcmp(phnumGet(pnum, 0), "02") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "02");
  assert(strcmp(phnumGet(pnum, 0), "02") == 0);
  phnumDelete(pnum);
  strcpy(num1, "10");
  strcpy(num2, "11");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "02");
  strcpy(num2, "22001");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1101");
  assert(strcmp(phnumGet(pnum, 0), "1101") == 0);
  phnumDelete(pnum);
  strcpy(num1, "0102");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "11");
  assert(strcmp(phnumGet(pnum, 0), "11") == 0);
  phnumDelete(pnum);
  strcpy(num1, "22");
  strcpy(num2, "200");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "01022");
  assert(strcmp(phnumGet(pnum, 0), "22") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "00110");
  assert(strcmp(phnumGet(pnum, 0), "00110") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "0200");
  assert(strcmp(phnumGet(pnum, 0), "2200100") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "12220");
  assert(strcmp(phnumGet(pnum, 0), "12220") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1011");
  assert(strcmp(phnumGet(pnum, 0), "1111") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "021");
  phfwdRemove(pf, "010");
  pnum = phfwdGet(pf, "11");
  assert(strcmp(phnumGet(pnum, 0), "11") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "121");
  strcpy(num1, "1");
  strcpy(num2, "102");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "020");
  strcpy(num2, "0012");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "01111");
  pnum = phfwdGet(pf, "11");
  assert(strcmp(phnumGet(pnum, 0), "1021") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "120");
  assert(strcmp(phnumGet(pnum, 0), "10220") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "11");
  phfwdRemove(pf, "22");
  phfwdRemove(pf, "222");
  strcpy(num1, "221");
  strcpy(num2, "122");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "101");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "010");
  strcpy(num1, "02100");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "122");
  assert(strcmp(phnumGet(pnum, 0), "10222") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "212");
  assert(strcmp(phnumGet(pnum, 0), "212") == 0);
  phnumDelete(pnum);
  strcpy(num1, "02111");
  strcpy(num2, "11201");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2021");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1112");
  assert(strcmp(phnumGet(pnum, 0), "102112") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "011");
  pnum = phfwdGet(pf, "1");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "0");
  assert(strcmp(phnumGet(pnum, 0), "0") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "211");
  phfwdRemove(pf, "1120");
  phfwdRemove(pf, "1");
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1002");
  assert(strcmp(phnumGet(pnum, 0), "1002") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2");
  assert(strcmp(phnumGet(pnum, 0), "2") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "10");
  assert(strcmp(phnumGet(pnum, 0), "10") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "22212");
  strcpy(num1, "21112");
  strcpy(num2, "11112");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "12");
  pnum = phfwdGet(pf, "22");
  assert(strcmp(phnumGet(pnum, 0), "22") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "0");
  assert(strcmp(phnumGet(pnum, 0), "0") == 0);
  phnumDelete(pnum);
  strcpy(num1, "02");
  strcpy(num2, "01000");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "11");
  strcpy(num1, "0");
  strcpy(num2, "00100");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "2");
  assert(strcmp(phnumGet(pnum, 0), "2") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2");
  strcpy(num1, "22112");
  strcpy(num2, "210");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "002");
  strcpy(num2, "2221");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0122");
  strcpy(num2, "20010");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "121");
  assert(strcmp(phnumGet(pnum, 0), "121") == 0);
  phnumDelete(pnum);
  strcpy(num1, "12");
  strcpy(num2, "2112");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0020");
  phfwdRemove(pf, "2122");
  phfwdRemove(pf, "0111");
  phfwdRemove(pf, "0002");
  strcpy(num1, "02211");
  strcpy(num2, "11");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "200");
  assert(strcmp(phnumGet(pnum, 0), "200") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2221");
  assert(strcmp(phnumGet(pnum, 0), "2221") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2");
  assert(strcmp(phnumGet(pnum, 0), "2") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1021");
  strcpy(num2, "22");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0");
  strcpy(num2, "20002");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "10110");
  strcpy(num2, "10222");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "00010");
  assert(strcmp(phnumGet(pnum, 0), "200020010") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "0");
  phfwdRemove(pf, "02");
  strcpy(num1, "0222");
  strcpy(num2, "01221");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0");
  assert(strcmp(phnumGet(pnum, 0), "0") == 0);
  phnumDelete(pnum);
  strcpy(num1, "22");
  strcpy(num2, "0102");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "11110");
  assert(strcmp(phnumGet(pnum, 0), "11110") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "10");
  strcpy(num1, "0");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "102");
  assert(strcmp(phnumGet(pnum, 0), "102") == 0);
  phnumDelete(pnum);
  strcpy(num1, "0");
  strcpy(num2, "11020");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "02102");
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  strcpy(num1, "12020");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "010");
  phfwdRemove(pf, "01");
  strcpy(num1, "00121");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1110");
  phfwdRemove(pf, "2120");
  phfwdRemove(pf, "012");
  phfwdRemove(pf, "00120");
  strcpy(num1, "02");
  strcpy(num2, "20012");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "102");
  assert(strcmp(phnumGet(pnum, 0), "102") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2");
  phfwdRemove(pf, "0020");
  pnum = phfwdGet(pf, "111");
  assert(strcmp(phnumGet(pnum, 0), "111") == 0);
  phnumDelete(pnum);
  strcpy(num1, "02011");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "212");
  assert(strcmp(phnumGet(pnum, 0), "212") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "12022");
  assert(strcmp(phnumGet(pnum, 0), "2112022") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "1222");
  phfwdRemove(pf, "0");
  strcpy(num1, "02102");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "010");
  phfwdRemove(pf, "20");
  phfwdRemove(pf, "0");
  strcpy(num1, "12120");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "00");
  strcpy(num1, "000");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "010");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "00222");
  strcpy(num1, "12110");
  strcpy(num2, "110");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1");
  strcpy(num1, "200");
  strcpy(num2, "0000");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "10100");
  assert(strcmp(phnumGet(pnum, 0), "10100") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "11202");
  assert(strcmp(phnumGet(pnum, 0), "11202") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "1200");
  pnum = phfwdGet(pf, "212");
  assert(strcmp(phnumGet(pnum, 0), "212") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "20");
  assert(strcmp(phnumGet(pnum, 0), "20") == 0);
  phnumDelete(pnum);
  strcpy(num1, "21122");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0");
  phfwdRemove(pf, "0102");
  phfwdRemove(pf, "2222");
  phfwdRemove(pf, "211");
  pnum = phfwdGet(pf, "2");
  assert(strcmp(phnumGet(pnum, 0), "2") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "11211");
  assert(strcmp(phnumGet(pnum, 0), "11211") == 0);
  phnumDelete(pnum);
  strcpy(num1, "21120");
  strcpy(num2, "1000");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "21");
  assert(strcmp(phnumGet(pnum, 0), "21") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  strcpy(num1, "211");
  strcpy(num2, "10");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "100");
  strcpy(num2, "02212");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0000");
  phfwdRemove(pf, "0011");
  phfwdRemove(pf, "00210");
  strcpy(num1, "120");
  strcpy(num2, "2122");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "201");
  assert(strcmp(phnumGet(pnum, 0), "201") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2010");
  assert(strcmp(phnumGet(pnum, 0), "2010") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "10");
  assert(strcmp(phnumGet(pnum, 0), "10") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "201");
  assert(strcmp(phnumGet(pnum, 0), "201") == 0);
  phnumDelete(pnum);
  strcpy(num1, "0222");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "11");
  phfwdRemove(pf, "00101");
  phfwdRemove(pf, "01");
  pnum = phfwdGet(pf, "2");
  assert(strcmp(phnumGet(pnum, 0), "2") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "110");
  assert(strcmp(phnumGet(pnum, 0), "110") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1221");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "10");
  pnum = phfwdGet(pf, "02001");
  assert(strcmp(phnumGet(pnum, 0), "02001") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "12001");
  strcpy(num1, "2");
  strcpy(num2, "0101");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "110");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "02");
  assert(strcmp(phnumGet(pnum, 0), "02") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "02");
  assert(strcmp(phnumGet(pnum, 0), "02") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2");
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  strcpy(num1, "002");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "022");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "01111");
  assert(strcmp(phnumGet(pnum, 0), "01111") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "11000");
  strcpy(num1, "01221");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "20");
  strcpy(num1, "12110");
  strcpy(num2, "0002");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "22");
  pnum = phfwdGet(pf, "122");
  assert(strcmp(phnumGet(pnum, 0), "122") == 0);
  phnumDelete(pnum);
  strcpy(num1, "11");
  strcpy(num2, "01011");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "102");
  pnum = phfwdGet(pf, "220");
  assert(strcmp(phnumGet(pnum, 0), "220") == 0);
  phnumDelete(pnum);
  strcpy(num1, "220");
  strcpy(num2, "022");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1");
  phfwdRemove(pf, "22");
  strcpy(num1, "0");
  strcpy(num2, "12210");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "101");
  assert(strcmp(phnumGet(pnum, 0), "101") == 0);
  phnumDelete(pnum);
  strcpy(num1, "22");
  strcpy(num2, "0021");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "01002");
  strcpy(num2, "112");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2222");
  phfwdRemove(pf, "11100");
  phfwdRemove(pf, "1");
  pnum = phfwdGet(pf, "12202");
  assert(strcmp(phnumGet(pnum, 0), "12202") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1012");
  assert(strcmp(phnumGet(pnum, 0), "1012") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "001");
  assert(strcmp(phnumGet(pnum, 0), "1221001") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1011");
  assert(strcmp(phnumGet(pnum, 0), "1011") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2002");
  assert(strcmp(phnumGet(pnum, 0), "2002") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "21");
  assert(strcmp(phnumGet(pnum, 0), "21") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "000");
  phfwdRemove(pf, "1011");
  strcpy(num1, "01012");
  strcpy(num2, "20001");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "220");
  strcpy(num1, "101");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "00022");
  strcpy(num1, "0010");
  strcpy(num2, "00111");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2120");
  phfwdRemove(pf, "0211");
  pnum = phfwdGet(pf, "112");
  assert(strcmp(phnumGet(pnum, 0), "112") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "11");
  assert(strcmp(phnumGet(pnum, 0), "11") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2012");
  assert(strcmp(phnumGet(pnum, 0), "2012") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "11");
  pnum = phfwdGet(pf, "21011");
  assert(strcmp(phnumGet(pnum, 0), "21011") == 0);
  phnumDelete(pnum);
  strcpy(num1, "0");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "21");
  strcpy(num2, "11");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "22111");
  assert(strcmp(phnumGet(pnum, 0), "0021111") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "20");
  assert(strcmp(phnumGet(pnum, 0), "20") == 0);
  phnumDelete(pnum);
  strcpy(num1, "12111");
  strcpy(num2, "0121");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "12020");
  assert(strcmp(phnumGet(pnum, 0), "12020") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "21201");
  assert(strcmp(phnumGet(pnum, 0), "11201") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "12220");
  phfwdRemove(pf, "211");
  strcpy(num1, "12");
  strcpy(num2, "2112");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "221");
  phfwdRemove(pf, "2");
  strcpy(num1, "1");
  strcpy(num2, "00");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "00");
  strcpy(num2, "002");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "22");
  assert(strcmp(phnumGet(pnum, 0), "22") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2");
  strcpy(num1, "0");
  strcpy(num2, "22");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "010");
  pnum = phfwdGet(pf, "01");
  assert(strcmp(phnumGet(pnum, 0), "221") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "02");
  assert(strcmp(phnumGet(pnum, 0), "222") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "21221");
  phfwdRemove(pf, "1");
  strcpy(num1, "11001");
  strcpy(num2, "000");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "111");
  assert(strcmp(phnumGet(pnum, 0), "111") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "11");
  phfwdRemove(pf, "2201");
  strcpy(num1, "02");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "2");
  assert(strcmp(phnumGet(pnum, 0), "2") == 0);
  phnumDelete(pnum);
  strcpy(num1, "211");
  strcpy(num2, "1021");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "00210");
  phfwdRemove(pf, "22101");
  phfwdRemove(pf, "02");
  pnum = phfwdGet(pf, "12100");
  assert(strcmp(phnumGet(pnum, 0), "12100") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "20212");
  assert(strcmp(phnumGet(pnum, 0), "20212") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "22");
  assert(strcmp(phnumGet(pnum, 0), "22") == 0);
  phnumDelete(pnum);
  strcpy(num1, "22");
  strcpy(num2, "12210");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "22");
  strcpy(num2, "0a22");
  assert(phfwdAdd(pf, num1, num2) == false);
  strcpy(num1, "02");
  strcpy(num2, "2122");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "101");
  phfwdRemove(pf, "12102");
  pnum = phfwdGet(pf, "21");
  assert(strcmp(phnumGet(pnum, 0), "21") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "12001");
  phfwdRemove(pf, "1");
  strcpy(num1, "0202");
  strcpy(num2, "202");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1");
  phfwdRemove(pf, "2");
  phfwdRemove(pf, "0");
  pnum = phfwdGet(pf, "0201");
  assert(strcmp(phnumGet(pnum, 0), "0201") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "212");
  assert(strcmp(phnumGet(pnum, 0), "212") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "110");
  phfwdRemove(pf, "121");
  strcpy(num1, "1");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "10102");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2");
  strcpy(num2, "1121");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "010");
  strcpy(num1, "212");
  strcpy(num2, "1121");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "10");
  strcpy(num2, "220");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "01201");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "11");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "000");
  assert(strcmp(phnumGet(pnum, 0), "000") == 0);
  phnumDelete(pnum);
  strcpy(num1, "12");
  strcpy(num2, "202");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0210");
  phfwdRemove(pf, "0011");
  pnum = phfwdGet(pf, "2");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "212");
  phnumDelete(pnum);
  phfwdRemove(pf, "20");
  strcpy(num1, "0");
  strcpy(num2, "0111");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "1");
  strcpy(num2, "22");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "201");
  strcpy(num1, "12");
  strcpy(num2, "1221");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "102");
  assert(strcmp(phnumGet(pnum, 0), "2202") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "01211");
  assert(strcmp(phnumGet(pnum, 0), "01111211") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "02");
  assert(strcmp(phnumGet(pnum, 0), "01112") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2");
  phfwdRemove(pf, "100");
  phfwdRemove(pf, "0122");
  phfwdRemove(pf, "1");
  strcpy(num1, "0021");
  strcpy(num2, "110");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "01021");
  strcpy(num2, "2202");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "12");
  pnum = phfwdGet(pf, "111");
  assert(strcmp(phnumGet(pnum, 0), "111") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "02000");
  assert(strcmp(phnumGet(pnum, 0), "01112000") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "11");
  phfwdRemove(pf, "220");
  pnum = phfwdGet(pf, "2");
  assert(strcmp(phnumGet(pnum, 0), "2") == 0);
  phnumDelete(pnum);
  strcpy(num1, "12001");
  strcpy(num2, "10");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "10");
  phfwdRemove(pf, "110");
  phfwdRemove(pf, "22");
  pnum = phfwdGet(pf, "11");
  assert(strcmp(phnumGet(pnum, 0), "11") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "20");
  assert(strcmp(phnumGet(pnum, 0), "20") == 0);
  phnumDelete(pnum);
  strcpy(num1, "12");
  strcpy(num2, "1110");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "20");
  strcpy(num1, "02021");
  strcpy(num2, "121");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0001");
  strcpy(num1, "20");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "22111");
  pnum = phfwdGet(pf, "111");
  assert(strcmp(phnumGet(pnum, 0), "111") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  strcpy(num1, "22");
  strcpy(num2, "12");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "22");
  phfwdRemove(pf, "10200");
  phfwdRemove(pf, "010");
  strcpy(num1, "0");
  strcpy(num2, "01");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "012");
  assert(strcmp(phnumGet(pnum, 0), "0112") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "1");
  phfwdRemove(pf, "22");
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "020");
  assert(strcmp(phnumGet(pnum, 0), "0120") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "0");
  pnum = phfwdGet(pf, "20211");
  assert(strcmp(phnumGet(pnum, 0), "02211") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "0222");
  strcpy(num1, "0010");
  strcpy(num2, "12");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "01");
  assert(strcmp(phnumGet(pnum, 0), "01") == 0);
  phnumDelete(pnum);
  strcpy(num1, "010");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "20");
  phfwdRemove(pf, "02200");
  phfwdRemove(pf, "00100");
  strcpy(num1, "2001");
  strcpy(num2, "10112");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "01");
  assert(strcmp(phnumGet(pnum, 0), "01") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "20021");
  phfwdRemove(pf, "1");
  phfwdRemove(pf, "1220");
  phfwdRemove(pf, "11122");
  phfwdRemove(pf, "1");
  pnum = phfwdGet(pf, "00");
  assert(strcmp(phnumGet(pnum, 0), "00") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2220");
  assert(strcmp(phnumGet(pnum, 0), "2220") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "120");
  strcpy(num1, "2202");
  strcpy(num2, "2201");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1000");
  assert(strcmp(phnumGet(pnum, 0), "1000") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2");
  strcpy(num2, "00220");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "11");
  strcpy(num2, "102");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "00");
  pnum = phfwdGet(pf, "0000");
  assert(strcmp(phnumGet(pnum, 0), "0000") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1");
  strcpy(num2, "20220");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0011");
  strcpy(num2, "010");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "220");
  assert(strcmp(phnumGet(pnum, 0), "0022020") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "001");
  assert(strcmp(phnumGet(pnum, 0), "001") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2");
  pnum = phfwdGet(pf, "11");
  phnumDelete(pnum);
  strcpy(num1, "21121");
  strcpy(num2, "121");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "12");
  assert(strcmp(phnumGet(pnum, 0), "202202") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "02");
  assert(strcmp(phnumGet(pnum, 0), "02") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "02");
  assert(strcmp(phnumGet(pnum, 0), "02") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "222");
  assert(strcmp(phnumGet(pnum, 0), "222") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "00");
  phfwdRemove(pf, "11222");
  phfwdRemove(pf, "11000");
  phfwdRemove(pf, "02211");
  pnum = phfwdGet(pf, "21010");
  assert(strcmp(phnumGet(pnum, 0), "21010") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2210");
  pnum = phfwdGet(pf, "02");
  assert(strcmp(phnumGet(pnum, 0), "02") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1002");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "222");
  strcpy(num2, "10000");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "221");
  strcpy(num2, "211");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "00002");
  pnum = phfwdGet(pf, "012");
  assert(strcmp(phnumGet(pnum, 0), "012") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "0212");
  strcpy(num1, "111");
  strcpy(num2, "20");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "00");
  assert(strcmp(phnumGet(pnum, 0), "00") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "12");
  assert(strcmp(phnumGet(pnum, 0), "202202") == 0);
  phnumDelete(pnum);
  strcpy(num1, "0220");
  strcpy(num2, "20");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "20112");
  strcpy(num1, "221");
  strcpy(num2, "00");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "10020");
  phfwdRemove(pf, "11010");
  strcpy(num1, "100");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "10010");
  phfwdRemove(pf, "01001");
  strcpy(num1, "20120");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "11202");
  phfwdRemove(pf, "0");
  pnum = phfwdGet(pf, "202");
  assert(strcmp(phnumGet(pnum, 0), "202") == 0);
  phnumDelete(pnum);
  strcpy(num1, "21010");
  strcpy(num2, "010");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "1011");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0");
  strcpy(num2, "011");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "12012");
  strcpy(num2, "001");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "21201");
  assert(strcmp(phnumGet(pnum, 0), "21201") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "21");
  phfwdRemove(pf, "0");
  phfwdRemove(pf, "10111");
  phfwdRemove(pf, "202");
  strcpy(num1, "1");
  strcpy(num2, "1201");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "10102");
  strcpy(num2, "220");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "2");
  assert(strcmp(phnumGet(pnum, 0), "2") == 0);
  phnumDelete(pnum);
  strcpy(num1, "21101");
  strcpy(num2, "21");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "002");
  assert(strcmp(phnumGet(pnum, 0), "002") == 0);
  phnumDelete(pnum);
  strcpy(num1, "22120");
  strcpy(num2, "0112");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "12021");
  strcpy(num2, "202");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "2");
  assert(strcmp(phnumGet(pnum, 0), "2") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "12");
  assert(strcmp(phnumGet(pnum, 0), "12012") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "12");
  assert(strcmp(phnumGet(pnum, 0), "12012") == 0);
  phnumDelete(pnum);
  strcpy(num1, "202");
  strcpy(num2, "11");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "200");
  strcpy(num1, "0");
  strcpy(num2, "211");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "000");
  assert(strcmp(phnumGet(pnum, 0), "21100") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "1011");
  phfwdRemove(pf, "1");
  strcpy(num1, "1");
  strcpy(num2, "0000");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "1");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "02022");
  assert(strcmp(phnumGet(pnum, 0), "2112022") == 0);
  phnumDelete(pnum);
  strcpy(num1, "21121");
  strcpy(num2, "022");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1001");
  phfwdRemove(pf, "212");
  phfwdRemove(pf, "2010");
  phfwdRemove(pf, "22");
  phfwdRemove(pf, "02");
  strcpy(num1, "00");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2001");
  strcpy(num2, "100");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "02");
  strcpy(num2, "02122");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "210");
  strcpy(num2, "2210");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1");
  phnumDelete(pnum);
  phfwdRemove(pf, "1");
  phfwdRemove(pf, "01");
  phfwdRemove(pf, "1210");
  pnum = phfwdGet(pf, "2011");
  assert(strcmp(phnumGet(pnum, 0), "2011") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  phfwdRemove(pf, "1");
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2");
  assert(strcmp(phnumGet(pnum, 0), "2") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "222");
  strcpy(num1, "0");
  strcpy(num2, "020");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "10");
  phfwdRemove(pf, "02");
  strcpy(num1, "0022");
  strcpy(num2, "102");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "11201");
  assert(strcmp(phnumGet(pnum, 0), "11201") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "0220");
  pnum = phfwdGet(pf, "2010");
  assert(strcmp(phnumGet(pnum, 0), "2010") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1020");
  strcpy(num2, "0221");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "212");
  pnum = phfwdGet(pf, "1221");
  assert(strcmp(phnumGet(pnum, 0), "1221") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "20");
  assert(strcmp(phnumGet(pnum, 0), "20") == 0);
  phnumDelete(pnum);
  strcpy(num1, "10021");
  strcpy(num2, "022");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "01110");
  strcpy(num2, "101");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  strcpy(num1, "021");
  strcpy(num2, "12010");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "202");
  phnumDelete(pnum);
  phfwdRemove(pf, "10");
  strcpy(num1, "02");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "212");
  pnum = phfwdGet(pf, "010");
  assert(strcmp(phnumGet(pnum, 0), "110") == 0);
  phnumDelete(pnum);
  strcpy(num1, "01");
  strcpy(num2, "0110");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1");
  phfwdRemove(pf, "2");
  pnum = phfwdGet(pf, "22");
  assert(strcmp(phnumGet(pnum, 0), "22") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "1202");
  pnum = phfwdGet(pf, "01");
  phnumDelete(pnum);
  strcpy(num1, "22");
  strcpy(num2, "01");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "001");
  strcpy(num2, "2201");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2");
  strcpy(num2, "20");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "22121");
  pnum = phfwdGet(pf, "11200");
  assert(strcmp(phnumGet(pnum, 0), "11200") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "1200");
  phfwdRemove(pf, "1222");
  strcpy(num1, "0");
  strcpy(num2, "201");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2");
  phfwdRemove(pf, "100");
  strcpy(num1, "0");
  strcpy(num2, "202");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "00001");
  strcpy(num2, "020");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1020");
  assert(strcmp(phnumGet(pnum, 0), "1020") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "220");
  pnum = phfwdGet(pf, "000");
  assert(strcmp(phnumGet(pnum, 0), "20") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "00");
  strcpy(num1, "02010");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0202");
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  strcpy(num1, "21011");
  strcpy(num2, "10");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0211");
  strcpy(num1, "100");
  strcpy(num2, "1212");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1001");
  assert(strcmp(phnumGet(pnum, 0), "12121") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "02122");
  assert(strcmp(phnumGet(pnum, 0), "1201022") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "01");
  phnumDelete(pnum);
  strcpy(num1, "20202");
  strcpy(num2, "1011");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "12");
  pnum = phfwdGet(pf, "20002");
  assert(strcmp(phnumGet(pnum, 0), "20002") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "12120");
  pnum = phfwdGet(pf, "2101");
  assert(strcmp(phnumGet(pnum, 0), "2101") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "101");
  assert(strcmp(phnumGet(pnum, 0), "101") == 0);
  phnumDelete(pnum);
  strcpy(num1, "010");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "002");
  strcpy(num2, "0201");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "101");
  strcpy(num2, "2201");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0010");
  assert(strcmp(phnumGet(pnum, 0), "202010") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "1120");
  pnum = phfwdGet(pf, "201");
  assert(strcmp(phnumGet(pnum, 0), "201") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "02");
  pnum = phfwdGet(pf, "112");
  assert(strcmp(phnumGet(pnum, 0), "112") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2");
  strcpy(num2, "211");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "22");
  strcpy(num2, "00220");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "221");
  assert(strcmp(phnumGet(pnum, 0), "002201") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "0211");
  strcpy(num1, "11");
  strcpy(num2, "10220");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "02");
  phfwdRemove(pf, "1002");
  strcpy(num1, "22");
  strcpy(num2, "1021");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "22");
  phnumDelete(pnum);
  phfwdRemove(pf, "01");
  strcpy(num1, "1");
  strcpy(num2, "01212");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "20210");
  assert(strcmp(phnumGet(pnum, 0), "2110210") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "22100");
  assert(strcmp(phnumGet(pnum, 0), "1021100") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "220");
  assert(strcmp(phnumGet(pnum, 0), "10210") == 0);
  phnumDelete(pnum);
  strcpy(num1, "21");
  strcpy(num2, "1022");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "20021");
  strcpy(num2, "1011");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "211");
  strcpy(num2, "1122");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "201");
  assert(strcmp(phnumGet(pnum, 0), "21101") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "001");
  strcpy(num1, "01");
  strcpy(num2, "0100");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "12");
  assert(strcmp(phnumGet(pnum, 0), "012122") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "01221");
  pnum = phfwdGet(pf, "01");
  phnumDelete(pnum);
  phfwdRemove(pf, "2022");
  phfwdRemove(pf, "1212");
  strcpy(num1, "0222");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "2");
  phnumDelete(pnum);
  strcpy(num1, "111");
  strcpy(num2, "01");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  phfwdRemove(pf, "1");
  phfwdRemove(pf, "1");
  strcpy(num1, "1011");
  strcpy(num2, "2101");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "01202");
  strcpy(num2, "202");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "01");
  strcpy(num1, "0011");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2222");
  pnum = phfwdGet(pf, "0101");
  assert(strcmp(phnumGet(pnum, 0), "202101") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "0221");
  phfwdRemove(pf, "0022");
  strcpy(num1, "1");
  strcpy(num2, "02022");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0");
  strcpy(num2, "21002");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2001");
  strcpy(num2, "001");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2220");
  pnum = phfwdGet(pf, "1120");
  assert(strcmp(phnumGet(pnum, 0), "02022120") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "12222");
  pnum = phfwdGet(pf, "011");
  assert(strcmp(phnumGet(pnum, 0), "2100211") == 0);
  phnumDelete(pnum);
  strcpy(num1, "01211");
  strcpy(num2, "00");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "22");
  phnumDelete(pnum);
  phfwdRemove(pf, "20");
  strcpy(num1, "2122");
  strcpy(num2, "02210");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "11");
  assert(strcmp(phnumGet(pnum, 0), "020221") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1");
  strcpy(num2, "11201");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "1012");
  strcpy(num2, "11022");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2");
  strcpy(num1, "2001");
  strcpy(num2, "22220");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "212");
  strcpy(num2, "201");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "002");
  phnumDelete(pnum);
  phfwdRemove(pf, "021");
  strcpy(num1, "10011");
  strcpy(num2, "00000");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "202");
  assert(strcmp(phnumGet(pnum, 0), "0202") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "10210");
  assert(strcmp(phnumGet(pnum, 0), "112010210") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2212");
  assert(strcmp(phnumGet(pnum, 0), "02212") == 0);
  phnumDelete(pnum);
  strcpy(num1, "222");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "1");
  strcpy(num2, "101");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "12");
  pnum = phfwdGet(pf, "1210");
  assert(strcmp(phnumGet(pnum, 0), "101210") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2");
  strcpy(num2, "21221");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "221");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0");
  pnum = phfwdGet(pf, "222");
  phnumDelete(pnum);
  strcpy(num1, "01");
  strcpy(num2, "2121");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "22");
  assert(strcmp(phnumGet(pnum, 0), "212212") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "020");
  pnum = phfwdGet(pf, "01");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "022");
  assert(strcmp(phnumGet(pnum, 0), "022") == 0);
  phnumDelete(pnum);
  strcpy(num1, "0120");
  strcpy(num2, "11");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "202");
  strcpy(num2, "2210");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0");
  assert(strcmp(phnumGet(pnum, 0), "0") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2112");
  strcpy(num1, "20211");
  strcpy(num2, "120");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "11001");
  assert(strcmp(phnumGet(pnum, 0), "1011001") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "21222");
  phfwdRemove(pf, "01012");
  strcpy(num1, "2");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "002");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "10");
  assert(strcmp(phnumGet(pnum, 0), "1010") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "01");
  phfwdRemove(pf, "0212");
  pnum = phfwdGet(pf, "00");
  assert(strcmp(phnumGet(pnum, 0), "00") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "201");
  assert(strcmp(phnumGet(pnum, 0), "001") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1");
  strcpy(num2, "21112");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1");
  strcpy(num1, "000");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "02");
  pnum = phfwdGet(pf, "202");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "111");
  assert(strcmp(phnumGet(pnum, 0), "111") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "0122");
  phfwdRemove(pf, "1");
  strcpy(num1, "00");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1120");
  assert(strcmp(phnumGet(pnum, 0), "1120") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "221");
  phfwdRemove(pf, "0220");
  phfwdRemove(pf, "012");
  pnum = phfwdGet(pf, "02");
  assert(strcmp(phnumGet(pnum, 0), "02") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "00212");
  phfwdRemove(pf, "11");
  pnum = phfwdGet(pf, "00");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "10122");
  assert(strcmp(phnumGet(pnum, 0), "10122") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1011");
  assert(strcmp(phnumGet(pnum, 0), "1011") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2");
  phnumDelete(pnum);
  strcpy(num1, "020");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "1200");
  strcpy(num2, "220");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "220");
  pnum = phfwdGet(pf, "21222");
  assert(strcmp(phnumGet(pnum, 0), "20122") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "12220");
  assert(strcmp(phnumGet(pnum, 0), "12220") == 0);
  phnumDelete(pnum);
  strcpy(num1, "0");
  strcpy(num2, "002");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2111");
  strcpy(num2, "21");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "211");
  pnum = phfwdGet(pf, "0011");
  assert(strcmp(phnumGet(pnum, 0), "011") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "011");
  phfwdRemove(pf, "21010");
  strcpy(num1, "02110");
  strcpy(num2, "120");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "00");
  strcpy(num1, "120");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "120");
  strcpy(num2, "20");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "02");
  strcpy(num1, "00");
  strcpy(num2, "121");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  strcpy(num1, "1");
  strcpy(num2, "012");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "02200");
  strcpy(num1, "120");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "00010");
  assert(strcmp(phnumGet(pnum, 0), "121010") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1");
  phnumDelete(pnum);
  strcpy(num1, "2121");
  strcpy(num2, "20122");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2");
  strcpy(num1, "121");
  strcpy(num2, "00100");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0");
  strcpy(num2, "1211");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "20");
  pnum = phfwdGet(pf, "2");
  assert(strcmp(phnumGet(pnum, 0), "2") == 0);
  phnumDelete(pnum);
  strcpy(num1, "202");
  strcpy(num2, "00011");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "1");
  strcpy(num2, "2202");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1");
  strcpy(num1, "20");
  strcpy(num2, "22");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "11102");
  strcpy(num1, "20111");
  strcpy(num2, "00221");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0200");
  pnum = phfwdGet(pf, "12");
  assert(strcmp(phnumGet(pnum, 0), "12") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "11");
  assert(strcmp(phnumGet(pnum, 0), "11") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2111");
  strcpy(num2, "22121");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "012");
  strcpy(num2, "02221");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "10212");
  strcpy(num1, "1201");
  strcpy(num2, "001");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "111");
  strcpy(num2, "1112");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "00");
  strcpy(num2, "101");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "002");
  assert(strcmp(phnumGet(pnum, 0), "1012") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2002");
  strcpy(num1, "200");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "12");
  assert(strcmp(phnumGet(pnum, 0), "12") == 0);
  phnumDelete(pnum);
  strcpy(num1, "02222");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "1");
  strcpy(num2, "111");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0012");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1");
  pnum = phfwdGet(pf, "2");
  phnumDelete(pnum);
  strcpy(num1, "21");
  strcpy(num2, "010");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  strcpy(num1, "12022");
  strcpy(num2, "22");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "10000");
  strcpy(num1, "202");
  strcpy(num2, "0110");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "22200");
  strcpy(num2, "010");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "21");
  strcpy(num1, "10110");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1101");
  strcpy(num1, "0");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "111");
  assert(strcmp(phnumGet(pnum, 0), "111") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "20021");
  assert(strcmp(phnumGet(pnum, 0), "221") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "100");
  strcpy(num1, "21222");
  strcpy(num2, "1222");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "2");
  phnumDelete(pnum);
  strcpy(num1, "2");
  strcpy(num2, "12202");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "101");
  strcpy(num2, "12");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1022");
  strcpy(num1, "002");
  strcpy(num2, "1020");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "1");
  strcpy(num2, "0002");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "120");
  assert(strcmp(phnumGet(pnum, 0), "000220") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "000");
  phfwdRemove(pf, "000");
  pnum = phfwdGet(pf, "1");
  phnumDelete(pnum);
  strcpy(num1, "0");
  strcpy(num2, "201");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "20");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "02");
  assert(strcmp(phnumGet(pnum, 0), "2012") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2");
  strcpy(num2, "011");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2");
  strcpy(num2, "2212");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "20101");
  phfwdRemove(pf, "12");
  strcpy(num1, "21");
  strcpy(num2, "0022");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "011");
  assert(strcmp(phnumGet(pnum, 0), "20111") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "21");
  pnum = phfwdGet(pf, "20002");
  assert(strcmp(phnumGet(pnum, 0), "202") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "10");
  assert(strcmp(phnumGet(pnum, 0), "00020") == 0);
  phnumDelete(pnum);
  strcpy(num1, "0");
  strcpy(num2, "20100");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0021");
  assert(strcmp(phnumGet(pnum, 0), "10201") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "10120");
  assert(strcmp(phnumGet(pnum, 0), "1220") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "110");
  assert(strcmp(phnumGet(pnum, 0), "000210") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "1000");
  phfwdRemove(pf, "0000");
  strcpy(num1, "122");
  strcpy(num2, "00");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1");
  pnum = phfwdGet(pf, "0200");
  assert(strcmp(phnumGet(pnum, 0), "20100200") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "210");
  assert(strcmp(phnumGet(pnum, 0), "210") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1021");
  strcpy(num2, "0102");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "011");
  pnum = phfwdGet(pf, "10");
  assert(strcmp(phnumGet(pnum, 0), "10") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "01210");
  strcpy(num1, "112");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0100");
  assert(strcmp(phnumGet(pnum, 0), "20100100") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1");
  strcpy(num2, "22");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "100");
  phfwdRemove(pf, "212");
  pnum = phfwdGet(pf, "0112");
  assert(strcmp(phnumGet(pnum, 0), "20100112") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "0021");
  assert(strcmp(phnumGet(pnum, 0), "10201") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "011");
  strcpy(num1, "1202");
  strcpy(num2, "12");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "11100");
  assert(strcmp(phnumGet(pnum, 0), "221100") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "220");
  phfwdRemove(pf, "1");
  phfwdRemove(pf, "221");
  pnum = phfwdGet(pf, "112");
  assert(strcmp(phnumGet(pnum, 0), "112") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2");
  pnum = phfwdGet(pf, "2211");
  assert(strcmp(phnumGet(pnum, 0), "2211") == 0);
  phnumDelete(pnum);
  strcpy(num1, "202");
  strcpy(num2, "2200");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0101");
  assert(strcmp(phnumGet(pnum, 0), "20100101") == 0);
  phnumDelete(pnum);
  strcpy(num1, "110");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "10");
  pnum = phfwdGet(pf, "01");
  assert(strcmp(phnumGet(pnum, 0), "201001") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "200");
  pnum = phfwdGet(pf, "10");
  assert(strcmp(phnumGet(pnum, 0), "10") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "21");
  assert(strcmp(phnumGet(pnum, 0), "21") == 0);
  phnumDelete(pnum);
  strcpy(num1, "120");
  strcpy(num2, "220");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0020");
  strcpy(num2, "22021");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "2");
  assert(strcmp(phnumGet(pnum, 0), "2") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "21020");
  strcpy(num1, "0");
  strcpy(num2, "01");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "1010");
  strcpy(num2, "112");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "00");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "2200");
  assert(strcmp(phnumGet(pnum, 0), "2200") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2112");
  phfwdRemove(pf, "11");
  phfwdRemove(pf, "10101");
  phfwdRemove(pf, "0001");
  strcpy(num1, "12");
  strcpy(num2, "002");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "000");
  strcpy(num2, "22");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "112");
  strcpy(num1, "1220");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "20022");
  assert(strcmp(phnumGet(pnum, 0), "20022") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "0010");
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "0210");
  assert(strcmp(phnumGet(pnum, 0), "01210") == 0);
  phnumDelete(pnum);
  strcpy(num1, "21");
  strcpy(num2, "1000");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "2");
  assert(strcmp(phnumGet(pnum, 0), "2") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1");
  strcpy(num2, "220");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "1201");
  strcpy(num2, "21010");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "22202");
  strcpy(num1, "1");
  strcpy(num2, "21211");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2221");
  phfwdRemove(pf, "01");
  phfwdRemove(pf, "01");
  strcpy(num1, "0");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0100");
  assert(strcmp(phnumGet(pnum, 0), "1100") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "120");
  phnumDelete(pnum);
  strcpy(num1, "11");
  strcpy(num2, "22110");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "02002");
  strcpy(num2, "00");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2220");
  strcpy(num2, "010");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0210");
  assert(strcmp(phnumGet(pnum, 0), "1210") == 0);
  phnumDelete(pnum);
  strcpy(num1, "2");
  strcpy(num2, "20012");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1");
  phfwdRemove(pf, "00");
  phfwdRemove(pf, "20010");
  pnum = phfwdGet(pf, "1011");
  assert(strcmp(phnumGet(pnum, 0), "1011") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "101");
  pnum = phfwdGet(pf, "10000");
  assert(strcmp(phnumGet(pnum, 0), "10000") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1000");
  strcpy(num2, "02010");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  phfwdRemove(pf, "0220");
  phfwdRemove(pf, "101");
  strcpy(num1, "11001");
  strcpy(num2, "01021");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "20201");
  strcpy(num2, "10");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2");
  strcpy(num2, "001");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0");
  strcpy(num2, "22111");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "122");
  phfwdRemove(pf, "0110");
  pnum = phfwdGet(pf, "01");
  assert(strcmp(phnumGet(pnum, 0), "221111") == 0);
  phnumDelete(pnum);
  strcpy(num1, "12");
  strcpy(num2, "110");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "00212");
  strcpy(num2, "22101");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "202");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "20221");
  assert(strcmp(phnumGet(pnum, 0), "220021") == 0);
  phnumDelete(pnum);
  strcpy(num1, "00");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2");
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  phfwdRemove(pf, "1210");
  pnum = phfwdGet(pf, "12100");
  assert(strcmp(phnumGet(pnum, 0), "110100") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "11");
  phfwdRemove(pf, "22");
  strcpy(num1, "2");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0120");
  pnum = phfwdGet(pf, "112");
  assert(strcmp(phnumGet(pnum, 0), "112") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "010");
  pnum = phfwdGet(pf, "012");
  assert(strcmp(phnumGet(pnum, 0), "2211112") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "1222");
  strcpy(num1, "11");
  strcpy(num2, "22100");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "2");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1220");
  strcpy(num2, "101");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0210");
  assert(strcmp(phnumGet(pnum, 0), "22111210") == 0);
  phnumDelete(pnum);
  strcpy(num1, "01020");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0011");
  strcpy(num2, "221");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0102");
  assert(strcmp(phnumGet(pnum, 0), "22111102") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "0200");
  strcpy(num1, "1");
  strcpy(num2, "001");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "22");
  strcpy(num1, "22");
  strcpy(num2, "212");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1");
  phfwdRemove(pf, "0212");
  strcpy(num1, "2");
  strcpy(num2, "21011");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "02");
  assert(strcmp(phnumGet(pnum, 0), "221112") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2001");
  phfwdRemove(pf, "001");
  strcpy(num1, "20");
  strcpy(num2, "112");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "20221");
  phfwdRemove(pf, "01010");
  strcpy(num1, "10200");
  strcpy(num2, "210");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2010");
  strcpy(num2, "02021");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0202");
  pnum = phfwdGet(pf, "2");
  phnumDelete(pnum);
  phfwdRemove(pf, "1000");
  phfwdRemove(pf, "2");
  phfwdRemove(pf, "121");
  strcpy(num1, "2");
  strcpy(num2, "010");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "11121");
  assert(strcmp(phnumGet(pnum, 0), "11121") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "00");
  strcpy(num1, "002");
  strcpy(num2, "220");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "00100");
  pnum = phfwdGet(pf, "0021");
  assert(strcmp(phnumGet(pnum, 0), "2201") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1020");
  assert(strcmp(phnumGet(pnum, 0), "1020") == 0);
  phnumDelete(pnum);
  strcpy(num1, "12212");
  strcpy(num2, "110");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "220");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "02");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  strcpy(num1, "2");
  strcpy(num2, "02021");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2100");
  strcpy(num1, "01");
  strcpy(num2, "21001");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "1001");
  strcpy(num2, "102");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0101");
  strcpy(num2, "121");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "01");
  strcpy(num1, "1122");
  strcpy(num2, "1001");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "01222");
  strcpy(num2, "0111");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0121");
  strcpy(num2, "11");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0211");
  strcpy(num2, "000");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "02");
  pnum = phfwdGet(pf, "02221");
  assert(strcmp(phnumGet(pnum, 0), "221112221") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "0102");
  phfwdRemove(pf, "2");
  phfwdRemove(pf, "0");
  strcpy(num1, "220");
  strcpy(num2, "01221");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "00002");
  assert(strcmp(phnumGet(pnum, 0), "00002") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "22020");
  assert(strcmp(phnumGet(pnum, 0), "0122120") == 0);
  phnumDelete(pnum);
  strcpy(num1, "1");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1");
  phnumDelete(pnum);
  strcpy(num1, "11100");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "11021");
  assert(strcmp(phnumGet(pnum, 0), "11021") == 0);
  phnumDelete(pnum);
  strcpy(num1, "11");
  strcpy(num2, "00212");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2112");
  phfwdRemove(pf, "11102");
  phfwdRemove(pf, "00220");
  strcpy(num1, "20");
  strcpy(num2, "21");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "0");
  pnum = phfwdGet(pf, "0221");
  assert(strcmp(phnumGet(pnum, 0), "0221") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "20");
  phnumDelete(pnum);
  phfwdRemove(pf, "2012");
  strcpy(num1, "022");
  strcpy(num2, "21002");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "11221");
  assert(strcmp(phnumGet(pnum, 0), "10011") == 0);
  phnumDelete(pnum);
  strcpy(num1, "0022");
  strcpy(num2, "112");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1002");
  assert(strcmp(phnumGet(pnum, 0), "1002") == 0);
  phnumDelete(pnum);
  strcpy(num1, "0012");
  strcpy(num2, "022");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "20210");
  strcpy(num2, "0211");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "000");
  pnum = phfwdGet(pf, "010");
  assert(strcmp(phnumGet(pnum, 0), "010") == 0);
  phnumDelete(pnum);
  strcpy(num1, "11");
  strcpy(num2, "120");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "010");
  strcpy(num2, "0211");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2");
  strcpy(num2, "12");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1012");
  strcpy(num1, "1212");
  strcpy(num2, "22");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "01202");
  phfwdRemove(pf, "212");
  strcpy(num1, "221");
  strcpy(num2, "110");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "20201");
  strcpy(num1, "21221");
  strcpy(num2, "20");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1");
  strcpy(num1, "020");
  strcpy(num2, "0211");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "011");
  strcpy(num1, "021");
  strcpy(num2, "1010");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "20");
  strcpy(num1, "22000");
  strcpy(num2, "011");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0021");
  strcpy(num2, "02112");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "02022");
  assert(strcmp(phnumGet(pnum, 0), "021122") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "02");
  pnum = phfwdGet(pf, "22020");
  assert(strcmp(phnumGet(pnum, 0), "0122120") == 0);
  phnumDelete(pnum);
  strcpy(num1, "221");
  strcpy(num2, "212");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "00001");
  strcpy(num2, "01222");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "02");
  strcpy(num2, "0212");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "01");
  assert(strcmp(phnumGet(pnum, 0), "01") == 0);
  phnumDelete(pnum);
  strcpy(num1, "21021");
  strcpy(num2, "0010");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "001");
  strcpy(num2, "220");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "20");
  phfwdRemove(pf, "212");
  strcpy(num1, "10");
  strcpy(num2, "1");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "21");
  assert(strcmp(phnumGet(pnum, 0), "121") == 0);
  phnumDelete(pnum);
  strcpy(num1, "02");
  strcpy(num2, "1000");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0");
  assert(strcmp(phnumGet(pnum, 0), "0") == 0);
  phnumDelete(pnum);
  strcpy(num1, "20220");
  strcpy(num2, "2110");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "022");
  pnum = phfwdGet(pf, "1001");
  assert(strcmp(phnumGet(pnum, 0), "101") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "1");
  phfwdRemove(pf, "0100");
  strcpy(num1, "0");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "1102");
  strcpy(num1, "0121");
  strcpy(num2, "0");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2");
  pnum = phfwdGet(pf, "1121");
  assert(strcmp(phnumGet(pnum, 0), "1121") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "102");
  strcpy(num1, "210");
  strcpy(num2, "10111");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "02");
  strcpy(num1, "011");
  strcpy(num2, "21122");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "01");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "210");
  pnum = phfwdGet(pf, "012");
  assert(strcmp(phnumGet(pnum, 0), "022") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  strcpy(num1, "020");
  strcpy(num2, "0000");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "00");
  assert(strcmp(phnumGet(pnum, 0), "00") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "002");
  phfwdRemove(pf, "0");
  phfwdRemove(pf, "20");
  strcpy(num1, "121");
  strcpy(num2, "2020");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "0");
  assert(strcmp(phnumGet(pnum, 0), "0") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "02202");
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "02212");
  assert(strcmp(phnumGet(pnum, 0), "02212") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "2110");
  pnum = phfwdGet(pf, "111");
  assert(strcmp(phnumGet(pnum, 0), "111") == 0);
  phnumDelete(pnum);
  strcpy(num1, "00011");
  strcpy(num2, "0202");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "01");
  strcpy(num2, "2022");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "20");
  strcpy(num2, "022");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "0");
  strcpy(num2, "1201");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "00011");
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "0101");
  assert(strcmp(phnumGet(pnum, 0), "202201") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "11");
  assert(strcmp(phnumGet(pnum, 0), "11") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "0122");
  assert(strcmp(phnumGet(pnum, 0), "202222") == 0);
  phnumDelete(pnum);
  strcpy(num1, "01200");
  strcpy(num2, "200");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "22");
  strcpy(num2, "1020");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "22212");
  assert(strcmp(phnumGet(pnum, 0), "1020212") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "0010");
  assert(strcmp(phnumGet(pnum, 0), "1201010") == 0);
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "1121");
  assert(strcmp(phnumGet(pnum, 0), "1121") == 0);
  phnumDelete(pnum);
  strcpy(num1, "0101");
  strcpy(num2, "0022");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2222");
  strcpy(num2, "1212");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "21");
  strcpy(num1, "21202");
  strcpy(num2, "12");
  assert(phfwdAdd(pf, num1, num2) == true);
  strcpy(num1, "2001");
  strcpy(num2, "2");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "200");
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  strcpy(num1, "21");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "00101");
  phfwdRemove(pf, "1211");
  strcpy(num1, "20");
  strcpy(num2, "022");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "1");
  assert(strcmp(phnumGet(pnum, 0), "1") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "0020");
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  pnum = phfwdGet(pf, "0");
  phnumDelete(pnum);
  phfwdRemove(pf, "11");
  pnum = phfwdGet(pf, "220");
  assert(strcmp(phnumGet(pnum, 0), "10200") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "0");
  phfwdRemove(pf, "001");
  pnum = phfwdGet(pf, "200");
  assert(strcmp(phnumGet(pnum, 0), "0220") == 0);
  phnumDelete(pnum);
  strcpy(num1, "11");
  strcpy(num2, "102");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdRemove(pf, "2");
  strcpy(num1, "0001");
  strcpy(num2, "120");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "020");
  assert(strcmp(phnumGet(pnum, 0), "020") == 0);
  phnumDelete(pnum);
  phfwdRemove(pf, "1");
  strcpy(num1, "022");
  strcpy(num2, "0112");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "00");
  assert(strcmp(phnumGet(pnum, 0), "00") == 0);
  phnumDelete(pnum);
  strcpy(num1, "111");
  strcpy(num2, "02");
  assert(phfwdAdd(pf, num1, num2) == true);
  pnum = phfwdGet(pf, "201");
  assert(strcmp(phnumGet(pnum, 0), "201") == 0);
  phnumDelete(pnum);
  strcpy(num1, "00");
  strcpy(num2, "11200");
  assert(phfwdAdd(pf, num1, num2) == true);
  phfwdDelete(pf);
}
